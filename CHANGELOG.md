# 更新日志

## 2026-09-24 ｜ fix: v3.3.4 薪酬预填 500 + 待续费分母单位；feat: 课时费双源核验、经营看板分店拆分、新版人员主档建档、炸弹会员清单、留资成交时间

### 修复

- **🔴 薪酬档案「系统预填」报 500（唯一约束冲突）**：档案的候选键是「姓名 + 门店」，
  跨店授课的老师**两店各一行**（`venue` 是工资所属门店，这是设计意图）；而三处写入方
  （`prefill`、`storeProfile`、旧 seeder）各自复制了一份「同名唯一就写 `user_id`」的判断，
  第二行把同一个 `user_id` 又写一遍 ⇒ `SQLSTATE[23000] 1062 Duplicate entry '53'
  for key payroll_profiles_user_id_unique` ⇒ 事务回滚 ⇒ **整个请求 500**。

  修法：三处写入方的账号绑定**收敛到唯一一处实现**（`PayrollController::distributeUserAccounts()`，
  配套 `accountsByNames()` / `composeNote()` / `saveProfileGuardingUserId()`），
  不再各留一份分叉的规则。

  实测：`POST /payroll/profiles/prefill` 由 500 变 **200**，`bindings={bound:4,unbound:1}`，
  库里**恰好两行、恰好一行带 `user_id`**（绿地店），按 `user_id` 分组无 `count>1`，
  连点第二次 `created=0`（幂等）。反证：把修复前写法重放，能真实复现那条 1062 报错（已回滚）。

- **「待续费」占比分母单位错误 —— `usage_total` 是金额不是次数**：`summarizeCards` 把
  已耗**金额**（元）直接加到 `residue_amount`（节）上，量纲混用。

  王素红「定制私教30节」：`residue=29`、`usage_total=466.63`、`curr_unit_cash_value=466.6333`
  ⇒ 旧算法 `bound = 29 + 466.63 = 495.63` ⇒ 占比 **5.85%** ⇒ 被误判为「紧急待续费」；
  新算法「已耗 = 466.63 / 466.6333 = 1 次」⇒ `bound = 30` ⇒ **96.67%**，不再提醒。

  「已耗次数」的权威规则收敛到 `consumedCount()` 单一入口：① 金额优先
  `usage_total / curr_unit_cash_value`；② 金额不可用时回退 `consume_amount_format` 文本；
  ③ 两者都不可用 ⇒ `null` **显式降级**，绝不编造分母。降级时分母只含剩余节数（保守偏高），
  且 `cards_list[].bound` 与汇总 `countBound` **同源同一表达式**。

  实测：王素红 `bound=30`、占比 **96.67%**、`renewal.in=false`；独立复算
  `round(466.63 / 466.6333) = 1` 吻合。

### 新增

- **薪酬计算 · 课时费双源核验**：原有课时费只依据「随心瑜预约记录」计价，
  现**并列取回「随心瑜课时记录」**（新增 `KyCourseRecordService`），两个来源同时下发、并显式暴露差额：
  `bySource`（两源各自合计）+ `diff`（恒等式）+ 差额告警 `COURSE_RECORD_UNAVAILABLE`。

  ⚠️ **只核验、不改计价**：`pricingSource` 显式声明为 `bookingRecord`，
  即计价仍以预约记录为准，课时记录用于对账。两源不一致时**不静默取单源**，而是把差额摆在界面上。

  实测（双店整月）：合计 **833 / 745（+88）**，绿地 **420 / 401（+19）**、东部 **413 / 344（+69）**。

  > 上游接口的**日期参数名是陷阱**：写成 `s_date` / `e_date` 时上游 `errno=0` 正常返回，
  > 但**静默忽略区间、返回全量历史**（实测 `count=646`，与完全不传日期的响应逐字相同）。
  > 本实现用 `start` / `end` / `page_index` / `page_size`，并有用例把「错名 = 静默返回全量」钉住。

- **经营看板 · 新媒体数据分店拆分**：按 **绿地店 / 东部店** 分别列出留资人数、到店人数、
  成交人数、线上成交率、到店奖励、核销提成（`data-test="media-split"`）。

  复用既有 `getDashboardSeries`（纯字段映射，**不写第二套聚合** —— 另写一份就会出现第二套口径），
  仅在「双店」视图渲染（单店视图再拆一次会与上方合计重复），不新增额外并发请求（双店恰 3 个）。

  口径：**留资按门店可加**（绿地 3 + 东部 2 = 合计 5）；**到店/成交按人去重，不要求两店相加等于合计** ——
  同一个人两店都有记录时，界面以 `≠` + 解释文案**显式呈现差额**，而不是把差额压掉或据此判失败。

- **按新版人员主档建档（55 人）**：新增 `PayrollMasterImporter`（纯解析、不碰 DB）+ `PayrollProfileFromMasterSeeder`
  （环境变量 `PAYROLL_MASTER_XLSX_V2`）。按**表头名**定位列，`FIELDS` 白名单 15 列
  **从机制上不含任何 PII 列**；身份标签归一化（`东部店:顾问` → `顾问`）；别名按 `,`、`，`、`、`、`/`、`;` 拆分；
  「两店固定底薪」从 `重点提醒` 列文本解析（**不硬编码姓名**）。

  与旧 seeder **并存不替换**：旧 seeder 吃 2026-07 旧交接包（`PAYROLL_MASTER_XLSX`），
  新版吃 2026-09 主档（`PAYROLL_MASTER_XLSX_V2`），谁在跑由环境变量决定；两者指向同一批人也不会产生重复行
  （按 `external_id` 定位）。**幂等**：主档留空的金额（`null`）不写入，不会把用户补录过的金额清成 0。

  > ⚠️ **PII 不入库**：主档含身份证号 / 手机 / 银行卡号 / 开户行，而 `payroll_profiles` 表**没有**这些列。
  > 解析侧的字段白名单从机制上就不取这些列；缺失的敏感资料**只在导入报告里以「缺几项」的计数形式告知**。

  实测：**55 行 / 有效 48 / 绿地 32 · 东部 23 / problems=[]**；缺口计数
  **手机 41 · 身份证 40 · 银行卡 5 · 企微 26**（seeder 自报与直读 xlsx 双向一致）；
  连跑两次 **新建 55 → 新建 0**；双底薪**恰好 3 人**。落库表 24 列**无 PII 列**，长数字串 0、手机号 0；
  并**反向证明源文件确有 PII**（长数字 51 处、手机号 15 条）—— 排除「因为源文件本来就没有」的假阴性。

- **会员管理 · 「炸弹会员」清单**：口径是「**钱收了、课没上、卡过期很久**」，
  四条同时成立：正式会员（复用 `scopeMemberCustomers()`）＋ 次卡（`type=1`，排除体验/员工/测试卡）
  ＋ 有余额（`residue > 0`）＋ 过期天数**严格大于 183 天**（`days > 183`，卡项可配置
  `bombExpiredDays`，默认 183）。

  实测（冻结样本，逐人比对零差异）：**302 人 / 393 卡 / 2509 节**。
  边界取法说明：严格 `> 183` 得 393 卡 / 2509 节；若改成 `>= 183` 会得 **394 卡 / 2512 节** ——
  差异就是「恰好过期 183 天」的那一张卡（到期 2026-03-25 · 精品小班 · 余 3 节 · `status=6`）。

- **留资管理 · 成交时间录入与展示**：编辑弹窗新增「成交时间」（日期选择器）；
  成交金额列下方以小字显示「成交 YYYY-MM-DD · 距留资 N 天」；无成交时间时显示占位符「**成交 —**」。

  修复一处**静默丢字段**：`LeadController::$leadFields` 此前有 `deal_amount` 却**没有 `deal_at`**，
  写入走 `array_intersect_key($values, $leadFields)` ⇒ 用户填的成交时间**被无声丢弃**。
  现已补入白名单，并在 `store()` / `update()` 加 `'dealAt' => 'nullable|date'` 校验。

  自动值 vs 用户值的边界（最易写错处）：非空用户值**优先、绝不被 `now()` 覆盖**；
  未传 / 传空且状态为「已成交」时**仍自动写 `now()`**（既有行为不回归）；
  显式清空天然生效。判定看「有无非空值」而非「键在不在」—— 前端提交整表单，`dealAt` 键恒存在。

### 口径与边界（如实说明，避免误读为行为变更）

- **过期卡的保留区**：保留区使**过期卡可查**并支撑**炸弹会员清单**；
  **P3 分支在合成场景可达，真实数据下 0 命中**。
  依据：`expire_date` **只取自 `$active`**（`KyMemberSyncService.php:413`），
  保留过期卡**不影响** P3；只有过期卡时落 **P5**。P3 的真实前提是 **`active` 卡 deadline 已过且超出回溯窗**，
  而**真实冻结样本中此类卡 = 0 张**（active 1042 张、usable 839 张、**已过期 0 张**）。
  ⇒ 本版**不改变**待续费清单的人数。

- **成交时间只精确到「日」**：编辑控件是 `ElDatePicker type="date"` + `value-format="YYYY-MM-DD"`，
  经其编辑后时刻归零（实测 `15:30:45` → `00:00:00`）。这是**刻意的日粒度设计** ——
  用户需求本身就是「设成交时间 → 算距留资天数」，日粒度正是需求。下游**全部按日粒度消费**
  （`AnalyticsController:154`、`PostClassReviewController:384`、`VisitMetrics:262-263`），统计不受影响。
  将来若确需精确到时刻，改动点在 `openEdit` / `save` 回传原始 ISO。

- **`leads.deal_at` 列早已存在**：由既有迁移 `2026_08_30_000009` 建好，
  因此本版**没有新增任何迁移**。

### 升级注意

**本版无需数据库迁移**（无新增/变更迁移文件；`update.sh` 里的 `php artisan migrate --force` 会执行但无迁移可跑，是空跑）。

若要**按新版人员主档建档（55 人）**，两条路任选：

1. **界面建档**（无需 xlsx）：用「课时费与身份标签 → 新增建档」，或「系统预填」批量生成 ——
   姓名与门店自动填好，身份标签与金额由用户补齐。
2. **主档导入**：把新版主档 xlsx 放到服务器（**不要放进仓库**），设 `PAYROLL_MASTER_XLSX_V2`
   指向该文件，然后跑 `php artisan db:seed --class=PayrollProfileFromMasterSeeder`。
   未设该环境变量时该 seeder 会**跳过并告警**，不会报错、不会建半截数据。

### 验证

- 后端全量 `php artisan test`：**643 passed / 21 skipped / 0 failed**（本地，含冻结基线用例）
- 前端 `pnpm build`（= `vue-tsc --noEmit && vite build`）：通过
- 端到端复现用户五个问题：**14/14 项通过**（含真实 HTTP、真实登录表单 + 真 Chrome、假上游 + 真实 `PayrollService`）
- 对抗性复核四轮全部 `pass`：待续费分母、预填绑定与建档、课时双源与看板拆分、留资成交时间
- 变异反证（改坏必须转红）：`deal_at` 移出白名单 ⇒ 7 例失败；`now()` 无条件覆盖用户值 ⇒ 3 例失败；
  日期参数名改成 `s_date`/`e_date` ⇒ 4 例失败；忽略 `$plan` 按文件行序绑账号 ⇒ 5 例失败；
  去掉「距留资」的上海时区归位 ⇒ 显示差一天

> ⚠️ **测试覆盖边界（如实披露，勿读作「已被测试覆盖」）**
>
> - 本项目**没有任何前端自动化测试基础设施**（`admin-web/` 下无 `.spec.*` / `.test.*`，
>   `package.json` 无 vitest / playwright / @testing-library / cypress）。CI 的前端门禁是**类型级**的
>   （`vite build` + `vue-tsc --noEmit`），**不含行为断言**。
>   ⇒ 经营看板「分店拆分」等前端功能**没有 CI 可复跑的行为守卫**，其验证依赖本地临时执行。
>   后端侧对分店拆分只有**间接**覆盖（口径与聚合在 `helpers.php` / `AnalyticsController.php`）。
>   将来接入自动化时可复用已埋好的选择器 `data-test="media-split"`。
> - `tests/` 下有 6 个文件引用**仓库外路径**（冻结样本 / `/tmp` 夹具）。这些文件在 CI 上会 **skip**
>   ⇒ **「本地全绿」≠「CI 全绿」**；且 **CI 绿 ≠ 冻结基线被验证过**（基线守护只在本地有效）。

## 2026-09-23 ｜ feat: v3.3.3 建档入口 + 系统预填；经营看板新媒体分店拆分与薪酬概括

> **归属更正（v3.3.4 补记）**：本条目③④⑤（建档入口 / 系统预填 / 待完善闸门）与
> 「薪酬概括」如述；但**「经营看板 · 新媒体数据分店拆分」当时落在了工作台首页、未落在经营看板** ——
> 该提交的 message 与当时的验证报告都声称新增在经营看板，实测 `3155878` 与 `41c2bd3` 两个提交里
> `admin-web/src/views/yimai/analytics/index.vue` **既无 `分店拆分` 也无 `media-split`（各 0 处）**。
> v3.3.3 确实做了这个功能，只是做在了**工作台首页** `today/modules/boss-dashboard.vue`（4 处），
> **放错了页面**。该表实际由 **v3.3.4** 在经营看板补齐，见上方 v3.3.4 条目。

### 新增

- **经营看板 · 新媒体数据分店拆分**（⚠️ **本条当时落在工作台首页、未落在经营看板，经营看板部分由 v3.3.4 补齐** —— 见上方归属更正）：
  原本只有一个总和，现增「新媒体数据 · 分店拆分」表，
  按 **绿地店 / 东部店** 分别列出留资人数、到店人数、成交人数、线上成交率、到店奖励、核销提成。

  后端 `/analytics/trends` 本就支持 `venue` 参数且 `computeMediaPerformance` 走
  `applyVenueScope`，因此**复用同一套口径**按店各取一次，不新写聚合（另写一份就会出现第二套口径）。
  仅在「双店」视图显示 —— 单店视图下再拆一次会与上方合计重复。
  实测两店相加等于合计（留资 5 = 2 + 3），可自校验。

- **经营看板 · 薪酬概括（含两店分别数据）**：在「薪酬计算」入口卡上增加概括表，
  三行：**合并（按所属门店） / 绿地店 / 东部店**，列出人数、总课时、应发合计、社保合计、个税合计、实发合计。

  > ⚠️ **两店相加 ≠ 合并合计**，这是规定口径不是重复计算。
  > `PayrollService::calculate()` 的候选集合包含**跨店授课的老师**（所属门店在一处、
  > 本月实际在另一处上课），引擎规定这类人必须同时出现在两家店的工资表里，
  > 否则她那部分课时费凭空消失（S1:161-164）。实测：合并 31 人、绿地 19 + 东部 17 = 36 人。
  > 界面上已写清这一点。

- **「课时费与身份标签」新增建档入口**：此前**只有 `PUT` 没有 `POST`**，界面上也没有任何新增按钮，
  档案只能由人员主档 xlsx（`PAYROLL_MASTER_XLSX`，仓库外高敏文件）导入 ——
  没配该文件的部署（含测试服务器）档案表是空的，主档之外的人**根本无从建档**。
  现新增「新增建档」按钮 + `POST /payroll/profiles`。

- **从系统已知信息预填建档**：`POST /payroll/profiles/prefill`（支持 `dryRun` 先预览，确认后才落库）。
  只扫**系统里真实出现过的人**，三个来源并集：

  | 来源 | 字段 | 说明 |
  |---|---|---|
  | 随心瑜预约 | `ky_bookings.teacher_name` | 真正上过课的老师及其门店（最可靠） |
  | 员工账号 | `users.name` + `venue` | 有登录账号的员工 |
  | 留资 | `leads.trial_teacher` | 留资里登记的上课老师（尚未产生课次的新老师） |

  姓名与门店**自动填好**；**身份标签留空、底薪/绩效/各项课时费一律 0**，由用户补齐 ——
  这些是算钱的输入，系统不替用户编。

### 修复

- **🔴 预填建档的判重差点造成数据事故（已修）**：第一版按「姓名 + 门店」字面判重，
  `dryRun` 立刻暴露问题 —— 系统里的 `teacher_name` **多半是别名**
  （冰璐→钱冰璐、芷晴/张芷晴→张情、苏米→罗柳柳、娟子→徐秀娟、婷婷→谭婷婷）。
  按字面判重会给别名再建一条档案，一个名字对应两个档案 →
  `PayrollNameResolver` 判定**歧义** → `resolve()` 返回 `null` →
  **本人从课时统计里整个消失**（本来明明是解析得到的）。

  改为用 `PayrollNameResolver::resolve()` 判重：命中既有档案（含**别名命中**的）一律跳过，
  并在 `skipped` 里说明命中了谁，供用户核对。实测 12 个候选降到 3 个真实新增。
  `test_prefill_does_not_break_existing_alias_resolution` 锁定该回归。

- **「待完善」安全闸门 —— 「字段留空」不等于「不参与计算」**：
  `payroll_profiles.role` 列有 `default('全职老师')`，而 `allowsBaseReward('全职老师')` 为真
  ⇒ 两店累计有效课时 ≥80 就发 **200~1000 元底薪奖励，这笔钱完全不看档案金额**；
  `allowsBaseSalary('')` 也为真。也就是说身份标签未知的人一旦进入计算，
  就会被静默按全职老师算出一笔看似合理的工资。

  新增 `pending_review` 列：新档案一律标 `true`，`calculate()` **整行跳过**，
  并进入 `unavailable` 清单**显式告知**（不静默少人）。保存时身份标签合法即视为已确认、自动解除。

  失败方向取「不算」而非「按默认算」：不算会被立刻发现（清单里有人、总额对不上）；
  按默认算则可能多发或少发工资且无人察觉。

### 升级注意

**须执行 `php artisan migrate`**（新增 `payroll_profiles.pending_review` 列）。
存量数据不受影响：`default(false)`，且「保存即确认」只对 `pending_review` 为真的行生效。

### 验证

- 新增 `PayrollProfileCreateTest` 15 例（建档 / 判重 / 权限 / 待完善闸门 / 预填幂等 / 别名解析回归）全绿
- 全量后端 **534 passed / 21 skipped / 0 failed**
- `vue-tsc` 0 错误、`vite build` 通过
- 存量 55 行档案 `pending_review` 全为 0，`calculate` 结果**逐字节不变**（31 人 / ¥90,985）
- 桌面 1440×900 与手机 390×844 实测三处界面均正常
- `check-mobile.js` 20/20、`check-overlays.js` 27 个弹窗 0 处 <44px

## 2026-09-23 ｜ fix: v3.3.2 体验课卡片「第undefined节」+ 卡片回写丢失（节次收口到模型）

### 修复

- **体验课卡片显示「第undefined节」**（桌面表格与手机卡片都有）：留资管理卡片上
  「体验课：第**undefined**节 芷晴（已上课）」——`session`（第几节）缺失时被直接拼进模板。

  **根因**：`session` 不是用户录入字段，而是**派生序号**（第几节 = 数组里第几个）。
  但一批历史卡片落库时没写这个键（只有 `date`/`topic`/`teacher`/`attended`），
  实测线上同形态数据 `has_session = 0`。缺了它之后，「谁来兜底」散落在 **5 个读方**，
  且**口径互相矛盾**：

  | 读方 | 原兜底 | 缺 session 时的实际结果 |
  |------|--------|------------------------|
  | 列表「第 N 节」（表格 / 手机卡片） | 无 | 渲染成「第**undefined**节」 |
  | `TodayController` 待办 key | `?? ''` | key 拼成 `trial:lead-6-`（无节次） |
  | `TodayController` 回写匹配 | `?? ($i + 1)` | 与上一行**相反** |
  | `api/yimai.ts` 今日待办映射 | `?? idx + 1` | 又一套 |
  | `leads/index.vue` 排序（跟进时限基准） | `?? 0` | 全部并列，取不到「第一节」 |

- **【同一根因，更严重】今日待办标记「已接待 / 已爽约」时卡片回写被整段跳过**：
  待办 key 少了节次 → `todoAction` 里 `if ($session > 0)` 不成立 →
  **卡片永远不落 `attended`/`noShow`**。表现是：老师在今日待办点了「已接待」，
  留资状态确实流转成「已体验」，但体验课卡片上的结果标记**丢了**（刷新也不出现）。
  新增用例先复现了这个失败：断言 `session` 期望 `1`、实得 `0`。

  **修法**：在**唯一 owner**（`App\Models\Lead`）加一个 **get-only** 的 `trialCards`
  Attribute 访问器，读出时对缺失/非法（`< 1`）的卡片**按数组位置补 `session`**；
  已有合法值的卡片原样返回，不重排、不改写用户数据。写入仍交给既有的 `array` cast
  （**不加 set**，避免两层职责重叠）。

  随后**删掉两个互相矛盾的兜底**（`TodayController` 的 `?? ''` 与 `?? ($i + 1)`）——
  访问器已保证键存在，再留兜底等于把「读到的形状」又猜一遍。

  **不涉及数据迁移**：补出的 `session` 是**归一化视图而非落库**，不回写、不动线上持久化数据；
  回写路径（今日待办标记）会把补好的值顺带落库，历史卡片**渐进自愈**（用例已断言）。

  **验证**：新增 `LeadTrialCardSessionTest`（3 例，先失败后通过）；全量
  **519 passed / 21 skipped / 0 failed**；`vue-tsc`、`vite build` 通过；
  实测 API 返回 `session = 1`，手机卡片与桌面表格的「第undefined节」均变为「第1节」，
  页面 `undefined` 计数归 0。

## 2026-09-23 ｜ fix: v3.3.1 手机端列表页可用宽度回归（卡片操作行被挤断）

### 修复

- **手机端列表页可用宽度缩水 24px（v3.3.0 引入）**：用户在测试服务器更新到 3.3.0 后反馈
  「留资管理页面手机端显示异常」。

  **根因**：v3.3.0 新增的统一页面范式里，`.list-page` 在手持设备（≤768px）下把内边距收紧成
  `padding: var(--list-page-pad-mobile)`（12px），但**媒体查询之外的 `.list-page--fill`
  已经声明过 `padding: 0`** —— 两者**权重相同（都是 0,1,0），后出现的赢**，而媒体查询里的
  12px 排在后面，于是 `--fill` 列表页（所有列表页）在手机上被**静默加回 12px 内边距**：
  每边各缩 12px、合计损失 **24px 可用宽度**。

  这不是「12px 太大」，而是**桌面 0 / 手机 12px 两档不一致**：卡片宽度按桌面端算好，
  手机端硬生生少 24px。

  **实测后果**（390×844，demo-super）：

  | 页面 | 卡片内容宽 | 卡片操作行 | 卡片高 | 整页高 |
  |------|-----------|-----------|--------|--------|
  | 留资管理（修复前） | 288 → **264px** | 1 行 → **2 行** | 397 → 449px | 6029 → 6811px |
  | 留资管理（修复后） | **288px** | **1 行** | 397px | 6059px |
  | 会员管理（修复前） | 288 → **264px** | — | — | 3032 → 4844px |
  | 会员管理（修复后） | **288px** | — | — | 3866px |

  - **卡片操作行被挤断**：卡片底部「编辑 / 变更记录 / 更多」三按钮为 `min-width: 84px` +
    `gap: 8px`；内容宽掉到 264px 后放不下 3 个（实测 114 + 142 + 84 = 340px > 264px），
    「更多」被挤到第二行、卡片变高 52px。
  - **分页器与卡片右边界错位**：手机端分页器居中于卡片 body，卡片变窄后出现
    「分页器已到中轴、卡片还有余量」的错位观感。
  - **换行阈值整体左移**：320 / 360 / 375 / 390 四个常见手机宽度**全部**从 1 行变 2 行
    （修复前只有 320 / 360 换行），414 及以上才开始正常。

  **受影响面**：全部 **10 个**使用 `.list-page--fill` 的列表页 —— 留资管理、会员管理、
  人员管理、客户经营池、任务管理、人员操作日志、系统日志、模型生成记录、价格审批、新客培养。

  **修法**：在手持设备分支内给 `.list-page--fill` **显式补回 `padding: 0`**
  （`admin-web/src/assets/styles/core/app.scss`）。该不变量在 `--fill` 分支的注释里本就写明
  是「必须」，只是在写手持设备分档时漏掉了；内容页（无 `--fill`）继续用 12px 收紧档，
  那档本来就是给它们准备的。

  **桌面端零影响**：桌面端 `.list-page--fill` 本来就是 `padding: 0`，本次只补手持设备分支，
  1440×900 下留资管理 / 会员管理 / 人员操作日志 / 客户经营池的卡片宽（1169px）、
  整页高（900px）、横向溢出（0）与修复前逐项一致。

  **验证**：`vue-tsc --noEmit`、`vite build`、`check-mobile.js`（10 页 × 手机/桌面 20 项全绿）、
  `check-overlays.js`（27 个弹窗、0 处 <44px）四道门禁全部通过。

## 2026-09-23 ｜ feat: v3.3.0 新增「薪酬计算」栏目 + 全站表格高度与 UI 统一 + 会员分层与「待续费」口径修复

### 新增

- **「薪酬计算」栏目（仅超管可见）**：新增「经营看板 → 薪酬计算」页与配套后端。
  - **老师课时数**：按**课次**去重统计（不是 `count(*)` 行数），并区分私教 45 / 60 分钟口径；
    与业绩表导入共用的别名解析一并落地。
  - **业绩表导入**：两店分别按月导入，导入时**剔除 299 活动卡**；幂等（重复导入不重复记账）。
  - **课时费与身份标签设置**：老师薪酬档案的读写界面（身份标签 + 课时费）。
  - **月度输入**：考勤、社保、个税；社保沿用「三态」语义（不因换月重置、跨店不继承）。
  - **薪酬计算**：按 Hermes 工资工作流口径计算，舍入为 `ROUND_HALF_UP` 逐笔处理。
  - **权限**：前端路由与后端 11 个端点均限超管，非超管请求返回 403。
  - **部署必读**：`PAYROLL_MASTER_XLSX` 若漏配，薪酬档案会**静默为空**（Seeder 缺文件只
    warning 不阻断）。升级时请确认该环境变量已配置，并确保 PHP 已装 `php_zip` 扩展。
    两条数据依赖用例在 CI 上 skip 属**预期行为**（本地有数据时才跑）。

### 修复

- **全站表格高度改为运行时实测（不再写死像素）**：改造前有 **12 处**表格写死
  `max-height`（口径：`<ElTable max-height="N">` 11 处 + `<ArtTable max-height="N">` 1 处；
  若按「所有数字字面量」口径则为 15 处，另含 2 处 `ElScrollbar` 与 1 处 CSS `max-height`）。
  每个数字都是「当时那一屏量出来的」，**没有任何一处知道表格上方/下方真实占了多少**，
  于是换分辨率就错位。
  - 现在统一由 `hooks/core/useTableHeight.ts` 运行时量出
    `容器可用高度 − 表格上方占位 − 表格下方占位`，页面**不写任何像素**。
  - 写死高度已清零（表格口径 12 → **0**）。
  - 附带修掉 `ArtTable` 的 `.art-table { height: 100% }` 导致**分页器被顶出卡片可视区**
    （实测客户经营池 `pagBottom=969 > 卡片可视底 884`，翻页要再滚一屏）。
- **`.el-alert` 内容增长后表格高度不重算（分页器被挤出视口）**：`useTableHeight` 的
  `ResizeObserver` 原先只观测表格自身 + 上溯祖先 + 下方兄弟，**漏了「表格上方、同一卡片
  body 内的兄弟节点」**。
  - 根因比「漏观测」更具体：`.el-alert` 由 `v-if` 在**挂观察者之后**才渲染，属于
    「挂载时还不存在的节点」，所以即使补了采集也仍采不到。
  - 修法：新增 `collectAboveElements()` 补齐上方采集，并新增 `watchStructure()`
    （`MutationObserver` 监听表格父链结构，出现新兄弟时重挂观察者），再用
    `sameElements()` 做元素身份闸门避免抖动。
  - 实测（会员管理，1440×900）：判定依据列表 1 → 7 → 13 条时 `max-height`
    491 → 365 → 239px，**漂移 0px**（修复前两步各漂移 126px、分页器分别越出视口 91 / 217px）。
- **内容页表格高度无界增长（本次新发现）**：同步页、数据备份页等「内容页」
  （`.list-page` 无 `--fill`，`overflow-y: visible`，高度随内容增长）被当成**定高容器**
  用「填满容器」计算，形成正反馈 —— 表格变高 → 容器变高 → 下一轮量到更大容器 → 表格再变高。
  实测同步页**每秒长 60px，30 秒后仍在长（306px → 2058px）**。
  - 修法：`findContainer()` 增加「内容页」判定，内容页改按**视口比例上限**约束
    （0.7，与改造前同步页写死 520px ÷ 740 ≈ 0.7 吻合）；弹窗分支公式保持不变。
  - 修复后同步页 / 备份页恒为 630px，8 秒连续采样无增长；备份页本地↔远端页签往返漂移 **0px**。
- **全站可用宽度回归**：`.list-page` 新增的 16px 全局内边距让原先内边距为 0 的
  `art-full-height` 系页面损失 32px 宽度。已按容器类型分档：`.list-page--fill { padding: 0 }`
  （恢复原有宽度），内容页保留 16px。实测留资管理 1440 / 1460px 筛选条由 2 行回到 **1 行**。
- **分页器与表格的间距回收 4px**：表格行高 72px，1440 / 1460 视口下表体 573px，
  差 3px 放不下第 8 行（8×72 = 576px）。间距由 16px 改为 **12px** 后表体 577px，
  **第 8 行回来**（`max-height` 636 → 640）。该间距抽成共用变量 `$pager-gap`，
  `.list-pager` 与 ArtTable 的 `.pagination` 共用同一个值 —— 只改一处会让
  「裸 ElTable + list-pager」和「ArtTable」两种写法重新分叉。
  分页器垂直位置不变（由卡片底部锚定），实测仍完整位于卡片内（`bottom=865 < 卡片底 885`）。
- **会员经营分层 P0-P4 恒为空**：分层不再只存在于展示层，改为**落库**并可重算。
  - **P5 语义修正（涉及授权面）**：P5 = 前端客资（留资），**不是**「最低价值会员」。
    语义错会导致**新媒体角色读到本不该可见的会员 PII**（`ky:` 来源的真实会员被算进 P5）。
  - 分层列与实时判定原先**分叉**（列表用落库值、详情实时算，两者可能不一致），已统一。
  - 口径边界：`type=member` → `layer != 'P5' OR external_id LIKE 'ky:%'`；
    `type=lead` → `layer = 'P5' AND (external_id IS NULL OR NOT LIKE 'ky:%')`。
- **「待续费」判定不精准（用户原始诉求）**：后端已算出 `renewalReasons`
  （`why` / `whyCodes` / `deadlineHit` / `bucket`）、`degraded` 降级说明、`coLists` / `primary`
  多清单归属，但**前端零消费** —— 用户「设置了判定因素却看不出为什么被筛进来」。
  现已全部接上：桌面端 popover、手机端内联呈现（刻意不同）。
  - 修掉「待开卡」guard 未考虑**余额未知的期限卡**、导致有未开卡卡项的会员其临期/过期
    期限卡被完全跳过判定的问题。
  - 卡项分类的证据集原先用「逐类列举」表达，`guard` 与 `hasAsset` 两处**各自漏了不同的一类**
    （连续三轮同一类缺陷）；已改为**正向判定**，结构性根因消除。
  - 「待开卡」文案不再硬编码（原先丢弃真实原因）。
  - 判定降级时页面顶部给出提示（「部分判定规则当前未生效，下面这些人可能被漏判」），
    不再静默。
  - 口径细节见 `docs/口径/会员卡项判定与待续费口径诊断.md`。
- **预流失徽标数与页签行数不一致**：`/customers/list-counts` 未复用 `/customers` 的
  `type=member` 过滤，把 P5 前端客资计入了会员管理页徽标（实测徽标 4、点进去 3 行）。
  已抽出唯一实现共用，徽标与页签不再各算一套。

### 界面与交互

- **统一页面框架**：页面根容器收敛为 `.list-page` + `--fill` 两档（原先 9 种写法）。
- **统一筛选条**：`.filter-bar` + 7 档宽度（与旧宽度一一对齐，桌面零像素变化）。
- **统一空态**：`:image-size` 的 50 / 60 / 70 / 120 四种写法统一为 64px。
- **统一分页器**：`.list-pager`（手机端居中）。
- **手机端触控目标补齐 44px**：新增裸 `.el-checkbox` / `.el-radio`
  （实测原先仅 32px 高：开通新账号弹窗 7 个角色/门店勾选框、
  会员管理评估弹窗的风险项）与 `.el-select__wrapper`（实测原先 24px 高）
  的触控兜底规则；`.el-tag__close` 由 14×14 撑到 44×44（EP 给它挂了关闭处理，
  手机端点不中就删不掉标签）。

### 已知行为变化（升级后请留意）

- **内容页的表格改为「表内滚动」，外层页面同时仍可滚动**。受影响的是**内容页**
  （`.list-page` 无 `--fill`，整页滚动）中行数超过上限的表格。实测全站仅 **2 页**出现
  「外层可滚 + 表内也可滚」并存：
  - `KeepYoga同步`：外层 664px + 表内 1653px
  - `课后分析`：外层 68px + 表内 1679px

  其余内容页（数据备份 / 训练计划 / 新客培养 / 人员管理 / 模型配置 / 营销工具 /
  谈单工具 / 工作台今日 / 今日待办 / 个人中心）**没有**嵌套滚动 —— 因为
  `max-height` 是**上限而不是定高**：行数不够时表格按内容自然高度渲染，
  不会凭空出现内层滚动条（实测训练计划 282px、新客培养 100px、数据备份 64px，均无内层滚动）。
  - **为什么保留这一形态**：把嵌套滚动去掉，要么让内容页的根容器不再滚动（内容页的东西
    本来就比一屏高，会直接看不到表尾），要么让超大表整张铺开（同步页 20 行 × 111px
    ≈ 2220px，用户要滚两屏才看到表尾）。当前形态是两者的折中，且**滚动会接力**：
    实测表内滚到底后继续滚动会顺滑接力到外层页面（同步页外层 400 → 664，
    课后分析外层 0 → 68），不存在「滚不动」的死区。
  - 同步页「历史同步批次」的表格上限由改造前的写死 520px 改为随视口自适应
    （1440×900 下 630px）；该表是**表内滚动**，不是行丢失。
  - 内容页的分页器需**滚动页面**查看（属整页滚动的正常行为，实测可完整滚入视口）。

### 内部质量

- **⚠️ 一条给后续维护者的约束：`mobile.scss` 里不要「按变体名」写触控兜底选择器。**
  同一个坑在**同一份文件里出现了三次**：
  1. §3 `el-input-number` 只写 `.is-controls-right`，但业务弹窗用的几乎都是默认变体
     → 规则整片落空，按钮停在 32×42；
  2. §4 只写 `.el-radio-button` / `.el-checkbox-button`，漏了裸 `.el-radio` / `.el-checkbox`
     → 68 处停在 32px；
  3. §4c 只写 `.el-select--default`，而弹窗里的 select 是 `.el-select--small`
     → 停在 24px。

  **约束**：兜底规则一律用**裸选择器**（如 `.el-select__wrapper`）兜住所有变体；
  需要提高权重时把各尺寸变体（`--default` / `--small` / `--large`）一并列出并显式加
  `!important`（EP 自身的 `--small` 规则权重 (0,2,0) 高于裸选择器 (0,1,0)，
  不加 `!important` 会继续被压住）。
- **门禁盲区（同一条约束的成因）**：弹窗触控门禁 `check-overlays.js` 的候选选择器只有
  `button / .el-button / [role=button] / a / .el-input-number__*`，**不含**
  `.el-checkbox` / `.el-radio` / `.el-select__wrapper`。所以这类缺口**门禁永远报通过**，
  不是「门禁绿了就没事」。本次已用扩展选择器独立扫描验证（27 个弹窗，同一时刻官方门禁报
  **0 处**，扩展选择器报 **68 处**，证实盲区真实存在；修复后扩展扫描降至 **0 处**）。
- **测试**：后端 `php artisan test` 全绿；前端 `vue-tsc --noEmit` 与 `vite build` 均通过。
- **独立验证**：见 `docs/验证/v3.3.0-验证报告.md`（16/16 验收项已证实通过）。

## 2026-09-21 ｜ fix: v3.2.2 课型映射纠错（v3.2.0/v3.2.1 把它改反了，本次改回）

### 修复

- **课型映射方向纠正（重要，涉及老师端可见范围）**：`course_type` 的数字↔课型
  对应被上一版**改反了**。正确口径是 **`2`=私教课**（对客显示「定制私教」）、
  **`3`=精品课**（对客显示「私教小班」）、`1`=团课；v3.2.0 起误改为 `2`=精品课、
  `3`=私教课，并附了一条重算迁移把历史 `course_kind` 按错误方向改写。
  本次改回正确方向，并新增迁移 `2026_09_21_000004` 按 `raw.course_type`
  **重新逐行重算**（幂等），把列值拉回正确口径。
  - **为什么这个必须修**：`course_kind='private'` 是授课老师「我的学员」与会员/客资
    可见性的**授权判据**。方向反了会导致两种后果同时发生 —— 老师的**真实私教学员
    从列表消失**，同时**把小班学员当作私教学员看到**（越权可见他人学员档案）。
  - **若已升级到 v3.2.0 / v3.2.1**：请尽快升级到本版以纠正数据；升级前照例先备份
    数据库。本版迁移只依据每行的 `raw.course_type` 重算，不猜、不默认，对真实同步行
    可完全复原。
  - 判定依据：用户第一手确认（核心床｜上肢线条雕刻 = 私教小班/精品课；
    VIP定制私教｜45Min = 定制私教/私教课）＋《随心瑜后台完整解读》同一次探索记录的
    逐行实测例子（前者 `type=3`、后者 `type=2`）＋ `docs/体验课课后分析-产品规划.md`
    的「小班行带 `course_type=3`」＋ 前端 `keepyoga.ts` 一直是本方向 ＋ 接口归属自洽
    （私教预约页返回 `type=2` 的课）。上一版依据的是把设置页**显示顺序**当成了数字
    绑定，属推断而非观测；完整复盘已写入 `courseKindFrom()` 注释，避免再次翻转。
- **迁移注释同步纠正**：`2026_09_15_000001` 与 `2026_09_21_000003` 的注释此前按
  错误方向写成「已废弃/反向」，现更正为与现行口径一致（两者代码均保持原样不改写，
  以维持「已执行迁移不改动」的纪律，数据修正一律由新迁移完成）。

## 2026-09-21 ｜ feat: v3.2.1 新媒体业绩机制 + 营销工具手机端适配（升级后请各门店重新开启一次 H5 分享）

### 新增

- **新媒体业绩机制（到店奖励 + 核销提成）**：口径按门店实际规则落地 ——
  新客从留资到**到店/成交超过 2 个月**即不计入新媒体新客成交；**每名到店体验新客奖励 20 元**；
  **当月线上新客成交率 × 当月核销金额** = 核销提成；**线下渠道不计入新媒体数据**。
  时效窗口取「留资当月 + 次月」（例：9.1 留资 → 10.31 前到店/成交有效）。
  提成系数分母为**当月全部到店新客**（含上月留资本月到店者）——
  例：10 月留资 20 / 到店 10 / 成交 6，另有 9 月留资 2 人在 10 月到店、其中 1 人成交，
  则到店奖励 `(10+2)×20 = 240 元`，提成系数 `(6+1) ÷ (10+2) = 58.33%`。
  统计口径为**自然月**（页面上其余指标为 30 天滚动窗口，卡片上已标注区间以免误读）；
  同一人有多个留资时取**最早的一条线上留资**；日期无法判定的一律**不计入并单独计数**，
  不猜、不默认有效。
- **留资管理「每页显示数量」**：可选 20/50/100/200 条（默认 50）。

### 修复

- **留资管理桌面端表格底部大片空白**：根因不是页大小，而是表格高度被写死 520px
  （上一版手机端适配时引入），一页 200 条也只显示 7 行。改为按视口高度自适应后
  空白 124px → 0，可见行数 7 → 9。
- **营销工具页手机端无法正常操作**（三个子工具）：常规指标全绿（无横向溢出、无小触控元素），
  但实测**按钮被裁切且点不中**（「生成朋友圈/历史生成记录/朋友圈库」合计 359px、容器仅 244px）、
  抽屉内记录列表被压成窄条、表单控件最窄仅 40px。现改为自适应卡片式：
  抽屉记录列表卡片化、触控目标全部 ≥44px、最窄表单控件 46px。桌面端逐值不变。
- **在线更新脚本增加防呆**：脚本此前在**开发仓库内**执行时，会把发行包 rsync 覆盖到 `backend/`，
  静默回退未提交的改动（本机实际发生过 4 次）。现检测到站点根或其父目录存在 `.git` 即中止，
  并在任何写操作之前退出；确需在检出内演练可用 `YIMAI_ALLOW_UPDATE_IN_REPO=1` 显式放行。
  **生产站点从发行包解出、不带 `.git`，不受影响。**

### 说明

- 本次包含数据库迁移（课型映射回补 + 分享启用列/归属列），**升级前请先做一次数据库备份**。
- 升级后请各门店重新开启一次 H5 分享（同 v3.2.0 说明）。

## 2026-09-21 ｜ fix: v3.2.0 销售分享越权收口 + 课型映射修正 + 手机端重设计（升级后请各门店重新开启一次 H5 分享）

### 安全

- **销售分享越权（信任根从客户端搬到服务端）**：对外分享码原先由前端一个**编译期常量**
  决定，任何拿到前端产物的人都能读出这个固定值并直接请求公开接口，学员案例（含健康、生育类
  描述）会随接口原文出网。现在分享码改由**服务端签发**（每次 16 位随机十六进制），并在库里记下
  这条码是**谁、以什么方式**签发的（`token_source`）。历史记录里那些**不是服务端签发**的码一律
  不再对外下发。
- **销售分享写路径越权**：超管 `publish` 会取「全表最新一条」并改写它（可能是他人行），原主人
  既看不到也停不掉自己已发出去的链接；超管 `disable` 不带目标时会**一次停用全库**；两个**同名
  账号**会互相接管对方的历史行。根因是「读可见范围」被当成了「写选择器」，现已拆开：写路径只认
  `created_by_user_id` 单键，同名歧义不参与写选择，归属改由管理员在「归属异常」面板处置。
- **分享内容清洗 fail-closed**：`payload` 只校验了 `array`，内层是任意结构，多塞的字段会原样进库
  并原样出公开接口。现按「对客页真正会渲染的字段」逐层白名单收口（顶层 + `share/info` +
  产品/教练/案例元素 + 阶段键），且**容器类型不符时丢弃该键**而不是保留原值 —— 否则换个容器
  （如把 `cases` 传成字符串）就能把未授权案例原文带出去。
- **`.gitignore` 加固**：避免本地环境文件与备份产物被误提交。

### 修复

- **课型映射修正**：`2=小班 / 3=私教` 原先映射颠倒（与随心瑜的真实口径不一致），并**回补历史
  `course_kind`**，避免既有排课的课型显示错误；同时把课型兜底收口到 `courseKindFrom()` 单一入口，
  不再各处各写一份。
- **phones 授权通道修复**：手机号关联判定恒为 `false`，导致**老师静默漏看**那些仅凭手机号关联到
  本人的学员（数据在库里，但页面上看不到）。
- **两处整页白屏崩溃**：`status.toLowerCase()` / `action.includes()` 遇到 `null` 时抛错致整页
  空白，现已加兜底。
- **数据边界归一化**：修掉 `undefined周` 一类把缺失值直接拼进文案的问题。
- **分享页缺数据白屏**：「数据不全」与「链接已失效」是两种状态——快照缺 `info`/`cases` 等键时
  改为**优雅降级**（不渲染该块 / 显示占位 / 隐藏无意义的电话与复制按钮），不再整页白屏；链接
  确实失效时仍显示失效页。

### 新增

- **孤儿分享处置入口**：分享的归属写入只认账号 id、且把「同名歧义」挡在写之外（必要的安全约束），
  代价是姓名对不上账号 / 归属 id 悬挂的历史行会变成**无人在线可管**的记录，若仍是启用状态链接
  照样能被客户打开。现「谈单工具 → 归属异常」（仅超管）可看到这类记录并选择**重新归属**或**停用**。
- **版本更新页常驻提示条**：把「升级后需门店配合的动作」放在超管一眼可见处，而不是埋在长日志里。

### 优化

- **手机端 UI 重设计**：6 个列表页的宽表格降级为卡片（此前左右固定列会吃满手机可见宽度、
  数据列完全看不见）、触控目标提到 44px、修掉弹窗内与窄屏的若干盲区与溢出。

### 说明

- **升级后各门店的 H5 分享链接会失效，请各门店重新开启一次分享。**
  - **现象**：谈单工具的分享开关仍显示「分享中」，但客户打开链接看到「链接已失效或已停用」
    （`GET /api/public/sales/{token}` 返回 404）。接口侧表现为
    `GET /api/shares/current` 返回 `enabled=true` 而 `token=null`。
  - **动作**：请各门店进入「谈单工具」，把 H5 分享开关**停用后再开启一次**（若已显示停用则直接
    开启即可），页面会重新发布并获得一条新的分享码，新链接立即可用。谈单工具页在检测到这种
    「开关是开的、链接已失效」状态时也会直接标红提示「分享码已失效，请重新开启分享」。
     存量里若有「姓名对不上账号 / 归属 id 悬挂」的历史行，请超管在
    「谈单工具 → 归属异常」里重新归属或停用。
  - **原因**：见上方「安全」第一条 —— 旧分享码可被任何人从前端产物读出，改服务端签发后来源不可信
    的码一律不再下发。这正是本次加固的目的，代价是历史链接需要各门店重新发布一次。
  - 重新开启是**幂等**的：存量行不会被删除（只是不再命中），且服务端会沿用已签发的码，
    因此重复操作只会拿到同一条链接，不会产生多条并存的对外地址。
  - 这条提示同样出现在后台「系统管理 → 版本更新」页（该页展示本文件，并另有一条常驻提示条）。

### 测试

- 销售分享相关回归 **104 项**（含归属/来源迁移的升级路径、发布完整性守卫、逐层白名单、
  容器类型不符时 fail-closed、同名账号互不接管、孤儿清单与处置）；
  发布完整性守卫会挡住「代码引用了未入库的迁移/测试」与「迁移或测试目录有未跟踪文件」两类问题，
  `update.sh` 也在 `rsync` 之前加了迁移完整性闸门。
- 全量后端 **325 项通过（1728 断言）**；前端 `vue-tsc` 类型检查通过。

## 2026-09-19 ｜ feat: v3.1.65 新客培养概览可点击筛选 + Gitee 上传改停滞超时

### 新增

- **新客培养概览卡片可直接点击筛选**：「新入会会员 / 待激活 / 待养成 / 已养成 / 有私教课 /
  有小班·团课」六张卡片现在点一下就把下方列表筛成这一类，再点一次取消；选中卡片高亮描边，
  下方显示「已筛选：待激活(0上课)（1 人）」与「清除筛选」。与上方的日期、门店、课型筛选
  **叠加生效**，分页跟随筛选结果，切页不会翻到空页。
  - 卡片数字改为**从当前列表算出**，不再用后端 `summary`。原因有二：一是后端 `summary`
    在勾了课型筛选时统计的仍是未筛选的全量，卡片数字会与点下去看到的行数对不上；二是
    后端 `summary` 的 `small + group` 是**两个「人数」相加**，同时上过小班和团课的人被算两次。
    改从列表算之后，「卡片上的数字 = 点下去看到的行数」严格成立（实测叠加课型筛选后
    3/0/3/0/3/0 与列表 3 行一致）。

### 工程

- **`make-release.sh` 打包成功后却报失败（并返回非 0 退出码）**：脚本末尾
  `echo "在线升级包: $ZIP（固定名副本 …）"` 里变量后面紧跟全角「（」，而它是多字节字符，
  bash 会把首字节并进变量名，在 `set -u` 下报 `ZIP…: unbound variable` 并以 127 退出。
  包其实已经打好了（4 个产物齐全），但任何检查退出码的调用方都会误判成打包失败。
  现变量一律写成 `${ZIP}`。**`update.sh` 第 18 行有同一处写法**，影响更大：站点目录配错时
  本该提示「请设置 SITE_ROOT 后重试」，实际只抛出一句 `unbound variable` —— 恰恰是最需要
  这条提示的场景。两处一并修掉。
- **Gitee 发行包上传超时改为「停滞超时」**：跨洋上传**成功**的一次本来就要好几分钟
  （实测安装包 10.9MB 约 440s、升级包 3.0MB 约 270s），所以不能拿「整个请求的总时长」当超时
  ——任何小于 ~450s 的值都会把「慢但成功」的上传判死。而原实现是 600s × 固定 5 次重试，
  真卡住时白烧约 52 分钟（整轮 CI 曾被拖到 62 分钟，也是历史上那些 27–32 分钟 run 被
  cancelled 的真实原因）。现把 multipart 请求体改为**流式按块发送**，socket 超时因此作用在
  「块与块之间」：只要还在推进就一直等，**连续 180s 无进展**才判死；安装包单独用更少的
  尝试次数（2 次），因为它最容易卡住、也最容易事后手工补传。所有阈值都可用环境变量覆盖。
  - **改流式 body 时的一个坑**：`http.client` 只要发现 body 有 `read()`，就会放弃算长度并改用
    `Transfer-Encoding: chunked`。原实现传 bytes 是带 `Content-Length` 的，换 chunked 是对上游的
    行为改变（部分服务端不接受分块上传），所以对 file-like body 显式补回 `Content-Length`。
  - 流式输出与原实现**逐字节等价**（已用空文件 / 1 字节 / 大文件、非对齐块读取逐一比对）。

### 说明

- **本次不改「出勤降低」的判定规则**。生产实测发现该清单只有东部店有数据、绿地店恒为空，
  根因是**绿地店的预约事实表 `ky_bookings` 缺了 2026-02-11 ~ 2026-08-25 一整段**：三个出勤
  窗口里 M1/M2 整个落在缺口内，`attend_m1 = attend_m2 = 0`，而规则是 `M1 > M2 > M3`，
  于是恒为假。上游随心瑜同期**有数据**（`venue_id=1` 查 M1 窗口返回 1779 行），属于「上游有、
  本地没落库」。**修复手段是在「数据同步」页执行一次「全量同步（重建）」**（增量模式只回溯
  最近 3 天，永远不会回头补历史），不是改代码。

## 2026-09-17 ｜ fix: v3.1.64 全量审计修复（越权收口 / 口径统一 / 升级可回滚）

一次覆盖后端安全、数据口径、前端产物与工程交付的全量审计修复。下面按「能不能被人利用」和
「数字对不对」排序，不按改动量。

### 安全

- **新媒体角色能看到双店全部留资**：`/leads` 列表只对店长/服务老师/授课老师调用
  `scopeLeadsForUser()`，新媒体落空，而 `scopeLeadsForUser` 对 media 又是「不加条件」并
  注释着"由调用方按场景叠加自己的过滤"——调用方没叠加。前端菜单「留资管理」对全部角色开放，
  所以这是点得进去的真实路径，返回姓名/手机号/微信/成交金额。现把 media 的口径（只看自己
  录入的客资）收回到 `scopeLeadsForUser` 内部，调用方不再需要"记得补过滤"。
  `/today/alerts` 与 `/today/todo` 走同一函数，一并修好。
- **前端演示种子数据进了发布产物**：入口 chunk 里躺着「王雅琴/郑好/许静姝…」一整套编造的
  客户名单（约 6.5KB）。种子是无条件调用的，打包器无法消除。现改为 `if (!USE_BACKEND)` 语句块
  （生产构建里 `USE_BACKEND` 是编译期常量 true，整块被消除），构建后实测包内已无任何假会员数据，
  入口 chunk 102.5KB → 96.0KB。
- **备份包默认包含 `.env`**：`keep_env` 默认为 true，而 `.env` 里是 APP_KEY（可解密全部加密
  字段、伪造签名 URL）、数据库口令、随心瑜账号与发布 Token；备份包会被上传到 WebDAV/S3。
  现默认关闭（前端开关同步改默认值，并加了一条风险提示），存量配置由迁移一并关闭。
- **S3 远端对象键未做文件名白名单**：本地路径一直有 `FILE_PREFIX` 正则校验，S3 侧直接拼接
  `$name`，带 `../` 可读到备份目录以外的对象。现三处（上传/下载/删除）统一走 `assertSafeName()`。

### 修复

- **留资备注删不掉，保存后刷新又回来**：`camelToSnake()` 结尾的
  `array_filter($out, fn ($v) => $v !== null)` 把「清空」当成「没传」丢掉了。前端清空输入框
  提交空串，Laravel 的 `ConvertEmptyStringsToNull` 先把它变成 null，键随即被滤掉 —— 控制器
  收到的请求看起来跟没改这个字段一样，于是提示保存成功、数据却没变。同一个 helper 的另一种
  写法（`($v[$f] ?? '') === '' → 置 null`）症状相反：字段没传也会被当成清空。现 `camelToSnake()`
  不再丢 null，清空语义统一交给新的 `normalizeEmptyValues()` 按列定义落地：可空列写 null，
  非空文本列写 `''`，非空日期/数字列保持不动（清不了），门店/来源/状态/负责人等归属列列入
  不可清空名单。同源的两个数据问题一并修掉：**老师只改备注会把成交金额抹成 null**、
  **只保存续课预报会顺手清掉最近沟通与生日**。回归用例见 `EmptyFieldPersistenceTest`。
- **老师工作台「我的客资」恒为 0**：`Collection::where($closure)` 会走 `Arr::where` 谓词路径，
  而 `staffOwnerFilter` 返回的闭包**没有 return 语句**，谓词返回 null 即假，全部元素被滤掉。
  改为 `filter(fn ($l) => staffOwnsRow(...))`。
- **老师工作台趋势图「客资数」恒为 0**：后端 `series[]` 只构造了 `classes`/`served`，前端靠
  `(p as unknown as { leads?: number }).leads ?? 0` 硬取——这个断言让 `vue-tsc` 检查不出来。
  现后端补 `leads` 列、前端补类型声明并去掉断言。
- **手机号查重静默漏报**：`/leads/check` 用用户原始输入查库，而全站其它 11 处都按纯数字归一。
  录入 `138-0000-0001` 时查重返回"无重复"，直接产生重复客资。现抽出 `normalizePhone()`，
  查重与落库都先归一（查询同时兼容历史里带分隔符的行），并加迁移把存量号码归一。
- **训练计划 PUT 会清空门店并夺走归属**：`update()` 没传 `$venue`，`attributes()` 把门店写成
  空串；同时 `created_by` 改成当前操作者。店长保存一次老师的计划，该计划就既不在老师列表、
  也不在本店列表（等于消失）。现与 `upsertOne()` 对齐：保留原门店与创建人，老计划缺门店时补当前门店。
- **归属判断的口径裂缝**：可见范围走 `staffNames()`（含别名），而准入校验 `canAccessLead` 仍
  只比规范名 —— 改过名或有别名的老师，其历史留资**列表看得见、点开改却 403**。现统一走
  `staffOwnsRow()`。`LeadController::destroy` 与课后分析的 `serviceOwns()` 一并收敛。
- **会员统计绕过角色范围**：`/analytics/summary` 与 `/analytics/trends` 的会员部分只卡门店，
  服务老师/授课老师看到的是**全店**会员数却标成"我的"；新媒体账号 `venue` 为空，
  `where venue is null` 恒为 0。现统一走 `scopeCustomersForUser`。
- **看板缓存会跨账号串数据**：缓存键只有「角色 + 门店」，同店两个服务老师会互相拿到对方的数字。
  非超管的键补上账号 id。

### 口径统一

- **新增 `VisitMetrics`**：到店/成交口径此前有两份独立实现，而且是**静默**不一致——看板侧是
  三来源并集（预约签到体验课 ∪ 留资状态 ∪ 体验课卡片已上课），老师侧只认留资状态一条，
  于是"老师只勾了卡片、没把状态推进到已体验"的到店在老师侧整批消失，同一页面上老板和老师
  看到两个成交率。v3.1.58/v3.1.59 两次都只修了看板侧，所以这次把口径本身抽成一份实现，
  两个调用方只传自己的范围（门店 / 本人）。老师侧实测：只勾卡片的客人现在会计入到店。
- **续费评估的近 30 天出勤改用同步的窗口定义**（含今天共 30 天）。此前用 `subDays(30)`（31 天），
  边界日算出的节数比会员表 `attend_m3` 多一天，同一个会员可能在两个页面一个进清单一个不进。

### 工程

- **在线升级有了回滚点**：`update.sh` 此前直接 rsync 覆盖现网，**没有任何备份或回滚分支**，
  migrate 失败就停在半新半旧。现在动文件之前先做两件事——调新的 `backup:snapshot` 命令生成
  数据库快照（`backup:run` 是给调度器用的，会判断"是否已到点/今天跑过"从而直接跳过，不能复用），
  以及打一份代码 tar；migrate 或迁移后健康检查失败时自动还原代码。快照保留最近 3 份。
- **`public/assets` 不再逐版本累积**：每次发版换一批内容 hash 文件名，rsync 不带 `--delete`
  所以只增不减（每次约 5MB，2H2G 的系统盘经不起）。现升级前先清空该目录再灌入。
  整棵树仍不加 `--delete`——包内没有 vendor，而服务器上还有宝塔 `chattr +i` 锁定的 `.user.ini`，
  一旦删除失败 rsync 会非 0 退出、在 `set -e` 下直接中断升级。
- **CI 增加质量门禁**：此前 CI 只做"能不能构建出包"，测试与类型检查全靠本地自觉
  （v3.1.51 的 MySQL-only 分支事故就是这类）。现新增 `test` job（跑 `php artisan test` 与
  `vue-tsc --noEmit`），`release` 依赖它。`phpunit.xml` 补了固定的测试用 APP_KEY，
  让测试不依赖本机 `.env`。
- **Gitee 发布的同名附件改为按内容校验**：此前只判"文件名是否已存在"，于是工作流被中途取消
  （旧包已上传）+ 手动重跑（新包重新构建）之后，Gitee 上的 v3.1.63 一直留着**已被 revert 的版本**，
  与 GitHub 的包不是同一个 commit，而 DEPLOY.md 把两者当同一份下载源。现按体积判断，变了就删旧重传。
  同时脚本失败不再 `exit(0)`——此前它把所有异常吞成成功，Gitee 连续 7 个版本没传上包都没人察觉。
- **备份恢复逐表校验行数**：先 truncate 再灌入，中途失败（超时/重复键/断电）会留下一张半空的表；
  现在每张表灌完立刻与 manifest 里的行数比对，不一致就报错并指出用哪份快照恢复。
  「恢复前快照」也改为在配置了远端时一并上传（本地盘出问题正是需要恢复的常见原因）。
- **备份的运行/恢复/校验补审计留痕**：恢复是超管侧权限最大的一步操作（整库覆写含账号与角色），
  此前不写审计。训练计划的四个写方法与课后分析的 `update()` 同样补齐。
- **缓存表不再无限增长**：database 驱动的过期键只是"读不到"，行会一直留在 `cache` 表里，
  而业务缓存键带版本号、每次写操作换一批新键。现由 `records:prune` 每日顺带清理过期行。
  五清单缓存的表指纹予以**保留**（它是"某条写入路径忘了失效"时的自愈兜底），
  改为给 `customers.updated_at` 补索引让其成本可忽略；同时补了
  `leads.trial_teacher_user_id` / `leads.created_by_user_id` / `ky_bookings.member_id|phone` 索引。
- **备份临时目录不再残留**：`rrmdir()` 只对第一层子目录做 rmdir，而备份工作目录是两层
  （`work-x/{data, files/public, files/private}`），第二层非空导致 rmdir 失败 ——
  每次备份都在 `storage/app/backups` 留一个空壳目录（本地实测已累积 411 个，生产同样只增不减）。
  现改为递归删除。

### 说明

- 本次**没有**改动的两件事，以及原因：
  - **新媒体仍可编辑/删除任意客资**：读侧已收窄为"自己录入的"，但写侧（`canAccessLead`、
    前端 `canManageLead`、`LeadController::destroy`）与既有测试
    `test_super_and_media_can_manage_leads_across_both_venues`（v3.1.17 加入）一致地把 media
    当作可代管客资的角色。读写口径不一致本身是待确认的产品问题，改动它会影响真实操作流程，
    因此留待业务确认而不是单方面收紧。
  - **`/analytics/*` 未加角色限制**：技术上任何登录账号都能调，但数据已按角色收窄，
    且前端「经营看板」菜单仅超管可见。
- 仓库历史里仍有四份商业文档（架构书 / 新媒体数据分析 / 经营助手报告 / KeepYoga 技能包）在
  旧提交中可下载，`docs/仓库历史清理与凭据轮换.md` 记录了完整处理方案（含凭据轮换清单）。
  该文档位于被 gitignore 的 `docs/`，不会入库。

### 测试

- 新增 16 项回归：新媒体客资列表只见自己录入的、待办只含自己的新客资、别名归属的客资可改
  （不只是可见）、手机号带分隔符可查重且落库归一、服务老师工作台的「我的客资」与逐日客资趋势、
  老师侧漏斗与看板同源（只勾卡片也算到店）、看板会员统计按人收窄且缓存不跨账号、
  备份结束后不留临时目录；以及本次新增的 7 项「清空字段必须落库」——
  留资备注清空、非空列清空写空串、店长整表提交只清被清的那一个、老师部分提交不误清成交金额、
  保存续课预报不误清最近沟通与生日、会员工作流文本清空、任务截止时间与验收标准清空。
- 全量 **157 项通过（824 断言）**；`vue-tsc` 通过；前端生产构建通过且包内无演示数据。

## 2026-09-16 ｜ fix: v3.1.63 留资「上课老师」列补全（此前一直为空）

### 修复
- **留资管理「上课老师」列一直为空**：体验课的老师实际填在**逐节卡片**
  （`trial_cards[].teacher`）里，而列表读的是顶层的 `trial_teacher` —— 那是旧版「单节体验课」
  模型留下的字段，代码注释也写着"仅兼容旧数据"。详情里逐节填了老师，列表自然一直是空的。
  - 列表列改为**从卡片取全部老师**（多节不同老师会显示"冰璐、婷婷"），没有卡片时才回落到顶层字段；
  - 后端保存时把第一节非空的卡片老师**镜像到顶层 `trial_teacher`**（跟着卡片走，卡片里改了这里同步改）；
  - 迁移把已有数据从卡片回填一次。
  - **镜像不只是为了显示**：`trial_teacher` 同时是老师的**归属字段**（`scopeLeadsForUser`
    用它判断"这条留资是不是我上的课"），只填卡片的话老师看不到自己上过的课。这一条一并修好。
- 移动端卡片同理补上老师名（原先只有节次与结果标记）。

### 说明
- **没有新增「体验结果」列**：列表的「状态」列本身就是 `已体验` / `爽约`，单开一列是重复。
  逐节的上课/爽约结果仍有地方看 —— 「体验课 / 券码」列会逐节标出「已上课 / 已爽约 / 已取消」。
- 顺带说明一处口径差异（不涉及改动）：卡片上的逐节结果与整条留资的状态是**两个粒度**，
  可能不一致（例如卡片已勾「已上课」，但状态仍停在「新留资」）。状态由人工推进，
  卡片结果由「今日待办 → 体验课」标记回写，两者本就不必同步。

### 测试
- 新增 2 项回归：保存时顶层老师取第一节非空卡片值且该老师能看到这条留资、卡片里改了老师顶层跟着改。
- 全量 **144 项通过（763 断言）**；`vue-tsc`、eslint 与前端构建通过。

## 2026-09-16 ｜ feat: v3.1.62 归属 id 落地 + 门店映射收敛，归属判断不再依赖单个姓名

### 新增
- **归属列落地 user id**：给 `leads`（顾问/上课老师/录入人）、`customers`（会籍顾问/负责人）、`ky_bookings`（上课老师）、`tasks`（负责人）、`training_plans`（创建人）、`post_class_reviews`（老师）共 6 张表补上 `*_user_id` 列，写入时与姓名**双写**，迁移按姓名/别名回填存量。id 不随改名变化、也不怕同名，是归属判断的长期依据。
- **归属映射面板新增「待清理：归属 id 指向已不存在的账号」**：账号删掉重建后会留下这类行（本人仍能看到数据，但 id 是悬挂的），列出来让管理员能清理。
- 管理员在「归属映射」里补完别名后会**自动回填历史行的 id** —— 迁移只在校验时跑过一次，这一步让"补映射 → 老数据归位"形成闭环，接口返回 `backfilled` 行数。

### 修复
- **归属判断改为「id 或姓名/别名取并集」**：中间版本曾写成「id 命中，或（**id 为空**且姓名命中）」，想避免"id 指明 A、姓名还写着 B"时两人同时看到。但那种写法有个静默得多的失败模式——**账号删掉重建后老数据的 id 悬挂，姓名明明对得上的人也永远看不到这批数据，而且不会被任何清单发现**（姓名能对上账号，不算未映射）。**丢数据与重复可见之间选重复可见**：重复至少能被发现。悬挂 id 由上面的待清理清单暴露，而不是靠查询时静默取舍。
- **门店 → 随心瑜 venue_id 的映射从 4 处硬编码收敛为配置**：原先散在 `KyController` 的三个方法里，**定时同步命令 `KyAutoSync` 里还有第四份**（这正是"加第三家店要改多处、漏一处就出现某店数据时有时无"的典型）。现统一由 `config/services.php` 的 `ky.stores` 提供（可用 `KY_STORES` 环境变量覆盖），helper `kyStores()` 读取。
- **`backfillStaffOwnerIds()` 按姓名回填时踩到类型陷阱**：`post_class_reviews.created_by` 与 `body_test_reports.created_by` 的**列类型本来就是 bigint（存 user id）**，不是姓名列，按姓名去比会直接抛 `Truncated incorrect DOUBLE value`。两处已从回填清单剔除，并加注释标明这两个例外。

### 说明
- 这一版是**加固不是替换**：姓名字段与别名匹配全部保留，id 只是多了一条更稳的依据。所以即使回填没覆盖到的历史行、或外部系统直接写姓名，也不会掉数据。
- 排查过程中发现的类型陷阱值得记一笔：**SQLite 不校验列类型，`bigint` 列里塞字符串照样通过**，所以"往 int 列写姓名"这类错误在本地 sqlite 测试里完全看不出来，只有真实 MySQL 才报。涉及列类型的改动务必在 MySQL 上验证一遍。

### 测试
- 新增 6 项回归：写入时双写归属 id、id 与姓名各命中一方时两边都能看到（并集口径）、id 缺失的历史行仍靠姓名可见、悬挂 id 能被列出、补别名后历史行自动回填、解析函数对同名/空值/查无此人一律拒绝。
- 全量 **142 项通过（755 断言）**；`vue-tsc`、eslint 与前端构建通过。

## 2026-09-16 ｜ fix: v3.1.61 人员归属改为「按账号解析姓名」，修掉昵称导致数据对本人消失

### 修复
- **老师改过昵称后看不到自己的客资/任务**：业务表的归属列（`leads.service_teacher` / `customers.consultant|owner` / `ky_bookings.teacher_name` / `tasks.owner` / `training_plans.created_by`）存的是**姓名字符串**，而 `/api/me` 返回的 `userName` 取的是**昵称**（`nickname ?: name`）。前端拿它写入归属列、后端按 `users.name` 过滤，两边不是同一个值：老师把昵称改成"小王"，之后新建的留资归属就写成"小王"，而后端只认"王教练"——**这条数据对该老师直接消失**。现拆成两个字段：`userName` 继续做展示名，新增 `staffName` 作为归属规范名，前端凡写入或比较归属一律用 `staffName`。
- **归属判断改成"按账号解析姓名"，不再只比一个字符串**：新增 `staff_aliases` 表（别名 → 账号），`staffNames()` 返回该账号在归属列里可能出现的**全部名字**（规范名 + 别名），全站归属过滤统一走它。这样姓名变过的历史数据也能正确归位：改名前写入的记录、随心瑜登记的是另一个姓名、历史昵称。迁移会自动把现有账号的昵称登记为别名，保证升级后老数据不断链。
- **别名全局唯一**：一个名字只能属于一个账号，撞车时保存直接报出冲突的名字，而不是在查询时静默算错（歧义归属比报错更危险）。

### 新增
- **人员管理的「归属映射」面板**：列出业务数据里出现过、但对不上任何账号的姓名（含出现在哪些表、各多少条），可直接映射到账号；也能逐个账号维护别名。**这个名单非空就说明有数据的归属是悬空的、本人看不到**，补一条映射即可修好——这是把"归属是否正确"变成可自查的关键。
- 接口 `GET /accounts/staff-mapping`、`PUT /accounts/{key}/aliases`（仅超管）。

### 口径说明
- **同一个人在不同系统里可能有不同姓名，这件事必须显式记录，不能靠猜。** 所以别名表是人工确认的映射，而不是自动模糊匹配——自动匹配在"张教练/张老师/张三"之间猜错一次，就会有人的数据归到别人名下，且很难发现。
- 本次收口的是**归属口径**（谁来匹配、如何唯一）；归属列的物理形态仍是姓名，没有改成外键。若要彻底免疫重名，下一步才是给各表补 `*_user_id` 列并按 id 过滤（`post_class_reviews.teacher_user_id` 已是先例）。

### 测试
- 新增 8 项回归：规范名与别名都算本人、别名归属的留资对本人可见、别人的名字仍不可见、别名撞车被拒、整体替换且忽略规范名与空值、未映射姓名能被列出（含"未分配"等占位符不算）、`/api/me` 的 `userName`（展示名）与 `staffName`（归属名）在昵称不同时必须分开。
- 全量 **136 项通过（733 断言）**；`vue-tsc`、eslint 与前端构建通过。

## 2026-09-16 ｜ feat: v3.1.60 移动端适配：宽表格降级为卡片、触控与弹窗基线、课后分析向导

### 新增
- **移动卡片组件 `MobileCard`**：手持设备上宽表格统一降级为卡片呈现（标题 / 副标题 / 标签 / 指标 / 说明 / 操作六段式，操作超过 2 个自动收进「更多」，按钮统一 44px）。组件只管呈现，字段取舍由各页面决定——每个页面的信息主次不同，按列配置自动生成的卡片只会产出平庸结果。
  - **为什么整体换形态而不是横向滚动**：业务表格普遍带左右固定列，而固定列不参与横向滚动，固定列一多就会把中间的数据列挤成零宽。实测会员管理总览 9 列内容宽 1265px、可见区仅 310px（左固定 130 + 右固定 260 = 390，已经超过可见宽度），**横滑也看不到数据**——屏幕上只剩「会员」和「操作」两列。留资管理更极端：16 列内容宽 1840px，要横滑近 6 屏。
  - 接入页面：会员管理、留资管理、新客培养、训练计划、课后分析列表、客户经营池、任务中心、价格审批、老师工作台（今日课程 / 我的相关客资）、今日待办。
- **断点单一来源**：新增 `src/config/breakpoints.ts`、`src/assets/styles/core/breakpoint.scss`（三档 640 / 768 / 1180，与 Tailwind 的 sm / md 对齐，这样手写 `@media` 与 `sm:` `md:` 工具类语义一致）与 `useDevice()` composable（模块级单例，全局只绑一次 resize 监听，避免每个组件各绑一个）。收敛原先散落在 16 个文件里的 9 个硬编码断点，其中重复三处的 `800` 统一为 `768`。
- **老师工作台新增手机端「填写分析」入口**：此前「今日课程」每一行没有任何按钮，老师下课要填课后分析只能走顶部入口。

### 优化
- **触控尺寸基线**：手机端全局控件高度 36px → 44px，并补上此前遗漏的 small 尺寸按钮组（课后分析里点选标签后出现的「轻 / 中 / 重」实测只有 35×24px，而老师是站着单手操作的）。
- **课后分析向导移动端**：表单标签置顶（原先 88px 的左侧标签吃掉可用宽度的近四分之一）、去掉每步 `max-h-62vh` 的嵌套滚动改为弹窗整体滚动（实测第一步内容高 1819px、可见滚动区仅 523px，且键盘弹起会与内滚动抢空间）、三步 `ElSteps` 换成「第 N/3 步」进度计数、**等级选择改为 44px 的底部面板并补上「取消选择该项」**（原先选中后没有地方可以取消）。标签块本身（胶囊换行、按组分类、带已选计数）是全站最移动友好的交互，未作改动。
- **今日待办**：分组切换在窄屏改为横向滚动 chip（8 个 `ElTabs` 在窄屏会退化成左右滚动箭头、只剩 2~3 个可见），待办行改两行布局，避免姓名被右侧状态标签与「处理」按钮挤到只剩几十像素。

### 修复
- **641–860px 区间弹窗横向溢出**：弹窗的移动兜底原先只在 `max-width: 640px` 生效，而项目里最宽的弹窗写死 860px（课后分析向导），iPad 竖屏这类区间会溢出。现改为无条件的 `max-width: calc(100vw - 24px)`：用 `max-width` 而非 `width`，所以小弹窗在手机上不会被强行撑满；不写媒体查询是有意的——任何宽度下只要弹窗比视口宽就该被收住。
- **触屏上点不开的两处菜单**：头像菜单与顶栏快速入口用的是 `trigger="hover"`，而触屏没有 hover。改为按设备切换（触屏 click、桌面保留 hover 的快速预览）。
- 移动端补 `env(safe-area-inset-bottom)`，避免全面屏底部内容被 home indicator 遮住。

### 说明
- 弹窗 / 抽屉内部的表格与操作列未改：弹窗已被全局限宽规则接管，优先级低于主列表。
- 超管专属的系统类页面（同步 / 备份 / 操作日志 / 模型记录 / 系统日志 / 人员管理）**有意不适配手机**——这些是坐在电脑前完成的工作，为它们做移动端只有成本没有收益。
- 桌面端零回归：9 个改动页面在 1440px 下仍渲染表格、卡片数为 0（已实测）。

### 测试
- `vue-tsc` 类型检查通过；生产构建零警告。
- 手机（390×844）实测 9 个页面：表格数 0、卡片正常渲染、无横向溢出，卡片按钮实测 44px；桌面（1440×900）实测同 9 页：卡片数 0、表格正常（1087–1119px）。

## 2026-09-16 ｜ fix: v3.1.59 训练计划按门店可见、分享页可打开、到店判定放宽

### 修复
- **训练计划只有创建人能看到**：`index` 统一按 `created_by = 本人姓名` 过滤，导致老师从课后分析转出来的计划**店长和超管一条都看不到**。现按角色的可见范围取：超管全部、店长本店、服务老师/授课老师自己创建的。
  - 为此给 `training_plans` 补上门店列（回填优先级：来源课后分析的门店 → 创建人所在门店），与其他模块口径一致；
  - 店长改了老师建的计划时**不夺走归属**：`created_by` 保持原样，否则店长一保存计划就变成自己的了。
- **训练计划分享链接打不开（提示已删除或已关闭）**：公开接口返回的字段里没有 `status` 和 `share`，而分享页正是靠这两个字段判断有效性——`p.status` 是 `undefined`，判断恒为无效，**这个页面从来没有成功打开过**。
  - 同时改造后只写入 `payload` 列、`content` / `profile` / `goal` / `images` 几个独立列不再写入，分享页读的恰是它们（即使打开也是空白）。现在这两处一并修正：公开接口以 payload 为基底、用数据库的权威字段覆盖；独立列也随 payload 同步维护。
- **线上到店人数偏少**：到店判定原先写成了「核销时间在区间，或者（核销时间为空且留资日期在区间）」。于是**上月买券、本月才到店**的客人（核销时间在区间外）会被排除——而这恰是线上到店的常见情形。现改为两个条件取「或」：核销时间与留资日期只要有一个落在区间就算。另补上第三个来源（留资里体验课卡片勾了「已上课」但整体状态未推进的），并在接口给出到店人数的来源构成 `visitBreakdown`，数字对不上时可直接核对是哪个来源漏了。

### 测试
- 新增 3 项回归：训练计划按角色可见范围（超管/店长/老师/跨店店长）、店长改计划不夺归属、到店判定对「核销与留资日期各在一处」的情形。
- 全量 **128 项通过（713 断言）**；`vue-tsc`、lint 与前端构建通过。

## 2026-09-16 ｜ fix: v3.1.58 登陆页精简、关闭多标签页、手机号全量显示、到店口径补漏

### 修复
- **线上到店人数仍偏少**：在原有两处来源（预约系统已签到的体验课、留资状态为已体验/已成交）之外，补上第三处 —— **留资里体验课卡片勾了「已上课」的记录**。老师经常只勾了卡片、没把整条留资的状态推进到「已体验」，此前只认状态会把这类到店整批漏掉。接口同时给出到店人数的来源构成（`visitBreakdown`），数字对不上时可以直接核对是哪个来源漏了。

### 优化
- **登录页精简**：去掉左上角与登录框上方重复的「一麦管理工作台」（只保留登录框上方那一处）；移除滑动验证 —— 原先必须拖动滑块才放行，对内部系统没有价值，还挡住登录。
- **默认关闭多标签页**：这是系统策略而不是用户偏好，因此把它从设置 store 的持久化里排除（`omit`），否则老用户浏览器里存着 `showWorkTab: true`，改默认值对他们不生效。
- **默认开启顶部进度条**：跳转时有进度反馈。
- **全局移除中英文切换**：系统只有中文，顶部栏语言入口停用，设置面板里对应项随之隐藏。
- **手机号全量显示**：店长工作台、客户经营池、新客培养、留资管理、课后分析等处此前只显示尾号（客户经营池还在打码），现统一显示完整号码；仅在确实没有完整号码时回退到尾号。顺带清掉一个名为 `maskPhone` 却直接返回原值的误导性函数。
- **设置面板的「菜单布局」不再因窗口窄而整块消失**：原先它带 `v-if="width > 1000"`，窄窗口下连标题都不渲染，看起来像「这一项点不了」。

### 说明
- 排查过程中确认：`showWorkTab: false` 本身不会破坏路由（此前一度误判）。真正会让人误以为「点了没反应」的是**内置浏览器对 SPA 内跳转的渲染滞后**——哈希已变、路由已正确匹配，但内容区仍是上一个组件；整页重载即正常。这个问题在未改动的提交版本上同样存在，属于测试环境特性，不影响正式浏览器。

### 测试
- 全量 **126 项通过（701 断言）**；`vue-tsc` 类型检查、lint 与前端构建通过。

## 2026-09-16 ｜ fix: v3.1.57 修复老师工作台 500、对客页打不开、转计划被覆盖

### 修复
- **老师端工作台整页报 500**：`TeacherOverview` 里身份去重集合用的是 Eloquent 集合，`intersect()` 会走 Eloquent 版 `getDictionary()` 对字符串元素调 `getKey()` 直接抛 `Call to a member function getKey() on string`。同一段代码还有一处更隐蔽的问题：查询没有 `select` 出 `phone`/`name`，而身份键正依赖它们，**会把所有人去重成同一个人**（到店、成交、成交率全部失真）。现已改为纯数组实现，并把手机号与姓名一并取出。
- **课后分析的对客页打不开**：分享链接少了 `#`。应用用的是 hash 路由，路径必须写在 `#` 之后，否则会被当成未匹配路径跳登录页。现与训练计划分享链接对齐：`${origin}${pathname}#/s/post-class/${code}`（流程页与列表页两处都改）。
- **转成训练计划后，到训练计划里找不到**：前端提交训练计划是「整表提交」，服务端旧实现为「删光本人全部再重建」。计划现在有两个创建入口（前端新建 + 课后分析流转服务端落库），只要前端手里是一份较早的列表（训练计划页还在另一个标签页开着、或本次会话早先加载过），一次提交就会把服务端刚生成的那份连带删掉。
  - **训练计划改为逐条读写**：新增 `POST /training-plans`、`PUT /training-plans/{id}`、`DELETE /training-plans/{id}`；批量接口保留但语义与单条一致（循环逐条 upsert），不再有「整表替换」；
  - 前端改为**只提交发生变化的行**（内容指纹与基线比对），没有任何改动时不打扰服务端；另一个标签页的改动也不会被牵连；
  - 删除改为**显式**：前端把真正删掉的 id 放进 `deletedIds`，服务端只删这些，不再用「不在提交列表里」推断；
  - 提交按 id 原地更新，保留 `source_review_id` / `source_body_test_id` 等不由前端维护的字段（旧实现全删重建会把这些字段一起丢掉）；
  - 新建计划的本地 id 与服务端主键可能不同（id 是全局主键，跨账号会撞车）：服务端在冲突时另分配主键并回传 `clientId → serverId` 映射，前端据此校正。

### 新增
- **课后分析按来源渠道区分「老会员」与「新建客资」**：老会员在会员系统已建档、本来就没有留资记录，旧实现只看留资，于是所有老会员都显示成「未建客资」，看起来像数据缺失。
  - 「上过课」列表新增来源列与筛选：**老会员**（会员系统已建档，展示主卡与剩余节数）／**新建客资**（只有留资，展示留资状态）／**未建档**；会员与客资两列列表也标注了各自渠道。

### 优化
- **留资管理列顺序调整**，并补上此前缺失的「备注」列。新顺序：留资日期 / 姓名·联系方式 / 需求·痛点 / 会籍顾问 / 上课老师 / 核销金额 / 成交金额 / 状态 / 备注 / 跟进时限 / 来源 / 下单平台 / 体验课·券码 / 门店 / 录入人 / 操作 —— 把跟进动作相关的列（金额、状态、备注、时限）提到前面，来源与平台等档案信息后置。

### 测试
- 新增 7 项回归：整表提交不得删除服务端生成的计划、显式 `deletedIds` 删除仍然生效、来源渠道能区分老会员与新建客资；训练计划逐条增删改、跨账号不可见、只提交变化的行不影响其他行、客户端 id 冲突时另分配主键并回传映射。
- 全量 **126 项通过（701 断言）**；`vue-tsc` 类型检查与前端构建通过。

## 2026-09-15 ｜ feat: v3.1.56 账号支持多角色叠加、体测报告接入训练计划链路

### 新增
- **账号支持多角色叠加**：人员管理的角色从单选改为多选，一个账号可以同时是服务老师、授课老师、店长等，**可见范围取并集**：
  - 服务老师 + 授课老师 → 名下会籍会员 ∪ 私教课学员
  - 再叠加店长 → 放大到本店全部（并集被更大范围吸收）
  - 写权限同理：任一角色够用即可（例如兼授课老师就能录课后分析）
  - 调整角色会立即失效该账号的登录态，重新登录后生效；账号列表同时展示全部角色标签
- **体测报告解析与归档**：老师把门店智能魔镜（ruleye）的报告链接贴进课后分析，系统解析出结构化指标并**自动带出训练观察项**，老师不用再逐项勾选。
  - 体成分 27 项：实测值 + 报告自带的标准范围 + 档位判定（档位名称、各档风险与建议均取自报告，无硬编码）
  - 体态 18 项：每项自带风险与建议，并映射到规则引擎的观察项
  - 健康问卷：产后、关节、手术、用药、眩晕等转入红线排查
  - 同一会员多次体测可归档为历史记录，供纵向对比
- **课后分析 → 训练计划流转**：确认后的课后分析可一键生成训练计划草稿，会员情况（年龄/性别/身高/体重/体脂）来自体测、关注要点用体测偏离项、三阶段方向直接带入；计划记录来源（哪次课后分析、哪份体测），便于回溯。
- **对客页展示体测数据**：学员版页面新增体测区块，展示指标、实测值与标准区间。

### 修复
- **同步/备份的完成时间比批次号早 8 小时**：批次号是写入时格式化的本地时间字符串，完成时间是 Carbon 序列化的 UTC ISO（`2026-09-14T19:00:00Z`），前端直接渲染导致两者看起来对不上——实际是同一时刻。新增统一时间格式化工具 `utils/date.ts` 按浏览器本地时区显示。
- **线上新客成交率的到店人数明显偏少**：两个原因叠加 —— 到店按「留资日期」归期而非到店日期（上月留资、本月到店的人被整段漏掉），且未按人去重（旧实现用 `max(留资侧, 预约侧)` 取较大值，等于默认两边数据完全重叠）。现改为：到店人数 = 区间内到店体验过的去重人数，来源同时取预约系统（已签到的体验课）与留资管理（已体验/已成交），按手机号合并；成交率 = 这些人里的成交人数 ÷ 到店人数，分子分母同源。全店口径同步修正。
- **今日待办的排课未按老师过滤**：老师端看到的是整店课表。现按角色收敛 —— 授课老师只看自己上的课，服务老师只看名下会员的课，店长/超管/新媒体维持原视图。
- **课后分析「客资状态」整列为空**：只按手机号匹配客资，而随心瑜预约行不保证带手机号。改为手机号 + 「姓名 + 门店」双索引匹配，并在预约行缺手机号时回填匹配到的会员/客资手机号。
- **会员出勤口径改为三个连续 30 天滚动窗口**：原实现按「近三个完整自然月」统计，**本月完全不计入** —— 会员本月天天来，出勤列仍显示 0；更实质的影响是「待续课」要求 `M3 > 0`（最近一个月有出勤），导致**上月休假、本月恢复训练且课时快用完的会员进不了清单**。现改为 M3 近 30 天（含今天）／M2 前 30 天／M1 再前 30 天，三个窗口等长且首尾相接（这是「出勤降低」按 `M1 > M2 > M3` 逐档比较成立的前提）。列头与看板卡片同步改为口径标签，并显示各窗口的实际日期范围。
- **同步时白跑一遍出勤计算**：`addAttendance` 在每批预约行的循环里算出勤，但同步末尾 `attendanceFromFacts` 会从预约事实表整段重算并覆盖，前者结果从未被使用，且每次同步都要解析一遍全部预约行。现改为只保留去重计数（同步结果展示用），出勤统一由末尾整段重算。
- **看板缓存键漏掉授权门店导致越权串数据**：`/analytics/*` 的缓存键未包含用户的授权门店列表，两个角色相同但授权门店不同的新媒体账号会命中同一条缓存、拿到对方的数据。现缓存键包含角色、门店与授权门店列表。

### 优化
- **课后分析可见人群扩展为三类**：上过课（体验课/私教课）、留资管理里分配给他的、约课系统里会籍顾问归属他的，页面顶部切换查看。
- **训练计划新建表单重排**：原来 6 列挤在一行（每格仅 1/6 宽），改成分组卡片 + 标签置顶；由课后分析带入的计划会在顶部提示来源，跳转过来时自动打开对应草稿。
- **设备风险文案的合规处理**：体测设备的风险/建议是通用医学口径，实测含门店表达禁忌词「矫正」，并有「可能导致腰椎间盘突出」这类接近诊断的表述。现分层处理 —— 老师端可参考设备原文并标记，**对客页一律不引用，只给指标、实测值与标准区间**。

### 测试
- 新增 29 项回归：多角色并集与店长吸收、改角色清 token、体测档位判定（含 `_range` 为 JSON 字符串、档位名含「标准」不得误判为正常、正常档不在中间）、判不出档位不得报警、设备原文禁忌词不进对客页、体测→课后分析→训练计划完整链路、红线记录不得流转成计划、看板缓存键隔离；出勤三窗口的边界与等长性、真实预约数据的分桶、今天上的课计入近 30 天、以及依赖它的三条清单规则（待续课/出勤降低/预流失）。
- 全量 **120 项通过（663 断言）**；`vue-tsc` 类型检查与前端构建通过。

## 2026-09-15 ｜ feat: v3.1.55 角色拆分为服务老师/授课老师，课后分析（P0）上线

### 新增
- **老师角色拆成两条线**：原来只有一个「老师」，职责混在一起。现拆为：
  - **服务老师（R_SERVICE）＝ 会籍顾问**：只看挂在本人名下的会员与客资，含待承接池（新留资待首响）。
  - **授课老师（R_TEACHER）＝ 私教主教练**：看自己上过私教课的学员，加上挂在自己名下的会籍会员。**小班和团课不计入「我的学员」**，这是明确的业务口径。
  - 人员管理可选择两种角色，二者都锁定单店；原 `R_TEACHER` 账号默认为授课老师。
  - 两个角色拆分的是**可见范围**，不是编辑字段：授课老师也可以兼会籍顾问，因此 `serviceTeacher` 仍可指派给自己；「不能指派给别人」「不能自己标记成交」两条控制保持原样。
- **体验课课后分析 ＋ 训练方向（P0）**：把老师提交的 Excel 方案落地成系统能力，入口在「业务执行 → 课后分析」，老师工作台也有带角标的快捷入口。老师当场勾观察项 → 系统生成三阶段方向 → 改一两处 → 确认后学员扫码带走。同时给出**照读提词卡**（今天练了什么 / 你今天最明显的进步 / 下节课推进什么 / 一句回家提醒）与**顾问衔接卡**（建议卡项方向、建议每周次数、已约下次课时间、顾问要同步的重点）。
- **规则引擎（非 AI）按「类型 × 本次观察」组装方案**：规则库来自老师方案里的「观察项→训练方向参考」等三张表，已全部数字化。同一学员类型下，观察项不同则三阶段重点、对客文案、回家作业都不同 —— 从数据流层面解决照抄预设导致的千人一面。规则引擎响应快、结果稳定、零调用成本，店长/教研可直接改文案。
- **观察项分两档**：当场只做 **10 项快筛**（看一眼就能判断、且直接影响方案），详细 31 项收进折叠区，已成交会员建档时用。目标：当场 5 分钟内填完。
- **红线做成硬拦截，不只是提醒**：勾选麻木/放射痛/进行性肌力下降、头晕胸闷、关节红肿热痛或夜间痛、产后盆底下坠或漏尿明显增加、近期外伤骨折术后未获运动许可 → **不生成对客训练方案**，改为「建议进一步专业评估」沟通卡；记录不进成交跟进池；对客页即使已确认也一律 404。老师只记录主诉与课堂表现，不做诊断、不解释影像、不承诺疗效。
- **对客页复用现有分享机制**：`/s/post-class/{code}`，免登录、手机版式，含「我在你身上观察到的」「三阶段方向（带次课换算）」「建议频率」「回家 1–2 件事」「下节课推进什么」与免责声明，支持复制全文/系统分享。
- **成交归因**：课后分析关联到客资，列表直接显示该学员是否成交与金额，为下一步「哪个老师、哪类学员、什么方向转化更好」提供数据基础。
- **接入今日待办**：新增「课后分析」分组，今天已签到的体验课与私教课会出现在老师的待办里（课型自动区分，团课不进），点「去填写」直达；填完自动从待办消失。服务老师不排训练，看不到该分组。

### 修复
- **老师工作台的课时是估算值**：此前 `上课节数 = 当日预约 × 0.6`、`服务人次 = 体验 + 预约 × 0.35`，源码注释自己写着"阶段2接入真实排课"。现改为读 `ky_bookings` 真实排课事实，并给出私教/小班/团课构成与「今日课程」表。
- **老师工作台的「所属会员」口径错误**：此前按 `customers.owner = 老师姓名` 判断，而 `owner`/`consultant` 是顾问字段、实际业务里不会填老师名字，老师大概率看到「所属会员 0 人」。现按角色正确口径统计，并区分「我的学员（私教）」与「我的会籍会员」两张卡。
- **课型无法区分私教与小班**：`ky_bookings.booking_type` 只有「团课/私教」两值，而小班（精品课）是从私教接口取的，`booking_type` 也是「私教」。新增 `course_kind` 列（private/small/group），带历史数据回填与索引；缺失 `course_type` 时按接口来源兜底（私教接口上的行算私教），避免老师的学员列表静默为空。
- **授课老师访问不了自己刚录入的客资**：客资录入后 `service_teacher` 为空、本人也不是顾问，按新的可见范围会被挡在门外。`canAccessLead` 补上 `created_by` 归属。
- **服务老师的顾问衔接看不到记录**：课后分析的关联 ID 缺失时（手工新建、排课行没匹配到会员/客资），顾问可见性与成交归因都会断。现按手机号 + 门店自动回填会员/客资关联，并在可见性判断里做同样的兜底。

### 优化
- **提词卡第②句不再套模板**：「你今天最明显的进步」是整段话术里最有杀伤力的一句，原来只能靠老师临场发挥。现由系统按命中的观察项给出具体到人的进步表述（31 项各有一条），老师改一下即可。
- **三阶段补上次课换算**：「1–2 个月」是教练语言，成交卖的是次卡。现在同时给出「每周 2 次，约 8–16 次课」，顾问谈次卡时能直接对应。

### 测试
- 新增 11 项回归：私教/小班/团课的学员可见范围、服务老师只读、红线硬拦截不产出方案且对客页 404、同类型不同观察项必须产出不同方案、老师改稿落库并透出到对客页、跨老师互相不可见、快筛项数量守住 8–10。
- 全量 **104 项通过（565 断言）**；`vue-tsc` 类型检查与前端构建通过。

## 2026-09-15 ｜ fix: v3.1.54 修正线上成交率口径与新媒体数据范围，定时同步留痕可见

### 修复
- **线上新客成交率分母用错**：新媒体运营板块（新媒体工作台、老板端）的「线上新客成交率」此前按「线上成交 ÷ 线上留资」计算，与「成交 ÷ 到店」的业务口径不符，数值明显偏低。现改为「线上成交 ÷ 线上到店」：分子为美团、大众点评、抖音、小红书、视频号、线上/团购来源在所选周期内的成交留资人数，分母为同口径留资中已到店（已体验/已成交）的人数；「线上留资→到店」转化率单独给出，`GET /analytics/trends` 同步新增 `onlineVisitCount` 与 `onlineLeadToVisitRate`。

### 优化
- **新媒体板块统一到「新媒体登记来源」单一口径**：新媒体工作台的留资人数、到店人数、成交人数、转化漏斗与留资/到店趋势图，全部只统计新媒体登记来源（美团/大众点评/抖音/小红书/视频号/线上/团购），不再混入全店自然到店等其他渠道；店长端保持全店各渠道合计并在卡片上明确标注，两侧口径差异一眼可见。
- **同步执行留痕到运行日志**：手动增量/全量同步与系统定时同步（成功、部分失败、失败）都写入「系统管理 → 系统日志 → 运行日志」，含触发方式、门店、批次、结果与耗时；定时同步因「当天已同步」或「全局锁占用」跳过时也留痕，后台能直接判断定时任务到底跑没跑、为什么没跑。同步历史批次表新增「触发方」列，区分手动操作人与「系统定时」。

### 运维
- **生产服务器补齐 Laravel 调度器**：`* * * * * php artisan schedule:run` 计划任务此前未配置，导致 `ky:autosync` 每日增量同步、系统日志保留策略清理、定时备份全部未执行（手工同步不受影响）。现已按 DEPLOY.md 要求补建并验证；后台「KeepYoga同步」页同时展示定时批次。

## 2026-09-12 ｜ fix: v3.1.53 修复自动备份无法关闭（配置接口字段命名错位致保存 422）

### 修复
- **自动备份开启后无法关闭、点「保存配置」报错**：备份配置接口后端返回下划线键名（`run_at`/`keep_local`/`keep_env`），而前端表单契约是驼峰（`runAt`/`keepLocal`/`keepEnv`）。页面每次加载或保存成功后表单按返回值回填，「备份时间/本地保留份数」实际为空；再保存（如关闭自动备份）时这两个字段被从请求中丢掉，后端校验 422（The run at field is required）——开关能切换但配置永远保存失败。现 `exportConfig()`/`status()` 统一按前端契约返回驼峰键名，内部存储格式不变，已保存的配置无需迁移。
- **状态总览卡片数据恒为空**（同一根因）：`next_run_at`/`last_run_at`/`local_count`/`local_size`/`remote_configured` 与前端 `nextRunAt` 等键名不匹配，导致已启用自动备份时「下次自动备份」也恒显示「自动备份未启用」、本地备份数量恒为 0、远端存储恒显示未配置，已一并修复。

### 测试
- BackupServiceTest 新增「配置接口驼峰契约 + 自动备份可关闭」回归用例（开启 → 回读 → 关闭全链路断言，复现修复前 422 场景）；后端全量 86 用例通过，并经本地端到端验证：开启自动备份后关闭保存返回 code 0（修复前必 422）。

## 2026-09-12 ｜ fix: v3.1.52 备份下载增加可见反馈，版本更新 502 给出排查指引

### 优化
- **备份下载可见反馈**：远端备份下载需服务器先从 NAS/网盘拉取再中转，期间页面无任何提示容易被误认为「点击没反应」。现下载按钮增加 loading 状态、远端下载提示「正在从远端拉取」，完成/失败均有明确提示；下载超时放宽至 5 分钟。
- **版本更新失败提示优化**：在线更新接口返回 502/504（nginx 连不上 PHP-FPM，宝塔小内存机器常见）时，给出明确排查指引（检查 PHP-FPM 运行状态、内存占用、PHP 错误日志），替代笼统的「Request failed with status code 502」。
- 经本地端到端验证：备份下载接口（HTTP 200 / application/zip / 合法 ZIP）与「立即备份 → 校验 → 恢复」全链路在真实 MySQL 环境正常；下载无反应与 502 属于服务器环境问题（外网连通性 / PHP-FPM 状态），需按运维清单在宝塔面板排查。

## 2026-09-12 ｜ fix: v3.1.51 修复立即备份报错（reset() 引用传参，MySQL 路径）

### 修复
- **点击「立即备份」报「reset(): Argument #1 ($array) must be passed by reference」**：备份数据表清单的 MySQL 分支使用 `array_map('reset', DB::select('SHOW TABLES'))`，`reset()` 要求按引用传参而 `array_map` 传值，PHP 8 直接抛错导致备份失败。改为取每行首个值的闭包写法。该分支仅在 MySQL（生产）下执行，自动化测试跑 SQLite（走 sqlite_master 分支）因此未覆盖——已在本地真实 MySQL 上补验备份/校验/恢复全流程（18 张表往返、恢复前快照正常）。

## 2026-09-12 ｜ feat: v3.1.50 数据备份新增 S3 兼容对象存储远端（OSS/COS/MinIO/R2）

### 新增
- **备份远端支持 S3 兼容对象存储**：「远端存储」新增 S3 类型，与 WebDAV 并列。备份上传/远端列表/下载/校验/恢复/删除/GFS 保留策略全链路可用，配置页可切换并分别测试连接。
  - 配置项：S3 端点、桶名、区域（默认 us-east-1，R2 用 auto）、AccessKey/SecretKey（均不回显、留空保持原值）、key 前缀（默认 yimai-backup/）、寻址方式（路径式 path 适配 MinIO/R2/B2，虚拟主机式 virtual 适配 AWS/腾讯云 COS）。
  - **零新增依赖**：AWS Signature V4 签名按规范用 PHP 原生实现（canonical request / HMAC 链派生密钥），复用 Laravel HTTP 客户端流式上传（sha256 分块校验、文件不经内存整读），与 v3.1.41 起「不引入 Flysystem 重依赖」的低配服务器约束一致。
  - 兼容对象存储：MinIO、腾讯云 COS、Cloudflare R2、Backblaze B2、AWS S3；阿里云 OSS 走其 S3 兼容模式（部分新账号受限时建议改用 WebDAV）。
- **密钥安全加固**：备份配置接口不再回显任何密钥明文（WebDAV 密码 / S3 SecretKey 读取恒为空、留空保存即保持原值），测试连接与保存均使用服务端留存值。

### 测试
- BackupServiceTest 新增 S3 用例：path 寻址上传落点（{endpoint}/{bucket}/{prefix}{name}）、AWS4-HMAC-SHA256 签名头、列表 XML 解析、测试连接；原「未配置提示」断言同步更新为 WebDAV/S3 通用文案。

## 2026-09-12 ｜ fix: v3.1.49 修复 WebDAV 测试连接报 500，改为返回具体失败原因

### 修复
- **备份页「测试连接」报 500「Server Error」**：v3.1.48 把测试中的失败当作异常直接抛出（未切换 WebDAV 类型/未填地址、NAS 地址无法解析、端口不通、连接超时、自签名证书校验失败等），生产环境统一显示为笼统的 500，真实原因被吞掉。现全部转为业务响应：前端弹窗直接显示具体原因，其中 cURL 网络层错误翻译为可操作提示（主机名解析失败 / 无法连接请检查端口与 WebDAV 服务 / 连接超时 / HTTPS 证书校验失败建议改用 http:// 或配置有效证书），同时详细错误写入系统错误日志便于排查。保存备份配置同样增加兜底。
- 顺带修复「远端未配置时点击测试」的误报：现在会明确提示「请先把远端存储切换为 WebDAV 并填写地址」。

### 测试
- BackupServiceTest 新增「WebDAV 失败以可读消息返回而非 500」用例（未选类型提示 + 自签名证书翻译），注意 cURL error 60 含子串 6、匹配顺序需先判 60。

## 2026-09-12 ｜ feat: v3.1.48 数据备份恢复（WebDAV/NAS）、同步增全量拆分与体验课待办修复

### 新增
- **数据备份与恢复（系统管理 → 数据备份，仅超管）**：
  - **备份范围**：全部业务数据表 + storage 私有文件（同步 CSV 快照等）+ 可选 .env 凭据；备份包为 ZIP（manifest.json + 每表 JSONL + 逐文件 sha256），JSONL 规避 SQL 转义与多行内容问题，MySQL/SQLite 通用；PHP 流式导出，不依赖服务器安装 mysqldump。
  - **导出/导入**：页面一键立即备份（仅本地 / 备份并上传远端）、下载备份包、上传备份包恢复；恢复为全量覆盖策略，执行前自动生成「恢复前快照」可回退，并先执行 migrate 将表结构升级到当前版本再灌数据；恢复需输入「恢复」二次确认，完成后需用备份内账号重新登录。
  - **远端存储（WebDAV）**：原生 HTTP 实现（PROPFIND/MKCOL/PUT/GET/DELETE），兼容群晖/威联通等 NAS、坚果云、Alist 桥接网盘；配置地址/账号/密码/远端目录（密码不回显、留空保持原值），带测试连接与目录自动创建；远端保留策略 GFS（7 天全留 + 4 周每周最新 + 6 个月每月最新），本地保留份数可配。
  - **自动备份**：复用既有 `schedule:run` 调度（每 10 分钟触发、按配置时间窗口判断并保证每天一次，默认 03:30），备份/恢复/手动同步互斥锁防并发；备份与恢复均为异步任务（受理即返回 + 轮询），超 130 分钟自动回收僵尸任务；全部动作写审计留痕，失败记系统错误日志。
  - **备份文件管理**：本地/远端备份列表，支持下载、**完整性校验**（逐文件 sha256 核对，远端包由服务器拉取后校验）、恢复、删除。
- **KeepYoga 同步拆分「增量 / 全量」两个按钮**：原「同步会员、卡项和预约」实为增量行为（会员/卡表全量核对，预约按上次同步回溯 3 天）但未明示，现拆为「增量同步（日常）」与「全量同步（重建）」——全量无视同步记录重新拉近两年预约，用于数据修复，执行前弹窗确认；后端 `/ky/import` 增加 `mode` 参数（非法值 422），批次显示名带「增量/全量」字样并入 metadata。

### 修复
- **已取消的留资体验课卡片不再进入今日待办**：留资里勾选「已取消」的体验课卡片此前仍会出现在当天的「今日待办 → 体验课」（随心瑜来源的预约是排除取消/爽约的，口径不一致）；现前后端聚合均跳过已取消卡片，改约后的新卡片不受影响。
- **在线升级脚本 PHP-FPM 重载名单补充 PHP 8.5 服务名**（php-fpm-85 / php8.5-fpm），与 v3.1.46 的 PHP 8.5 环境对齐。

### 测试
- 新增 BackupServiceTest 4 个用例：备份→改数据→恢复的数据往返（含安全快照）、篡改备份包拒绝恢复（校验和不匹配）、WebDAV 上传/列表 mock、本地保留策略；TodoActionTest 新增「已取消体验课卡片隐藏于今日待办」用例；KyMemberSyncRegressionTest 新增「同步模式控制预约拉取区间」用例。

## 2026-09-12 ｜ feat: v3.1.47 朋友圈防折叠改写、体验课待办联动留资与字数自定义

### 新增
- **朋友圈防折叠改写**：营销工具 → 朋友圈在文案生成下方新增「朋友圈防折叠」卡片。同一条文案换号再发会被微信折叠成「一条相似内容」，把会被折叠的文案粘贴进来，大模型按防折叠规则改写出「意思不变、气质不变、说法不同」的版本：开头句必换、至少 60% 句子重写、任何位置不出现连续 10 字相同、核心信息（价格/时间/联系方式/链接）原样保留、篇幅 ±20%。支持可选补充要求（如「语气更口语一点」）、一键带入上方生成结果；自带质检——自动计算结果与原文的最长连续相同片段，≥10 字红字提醒重新改写。改写结果计入历史记录、用量统计与审计（新 featureType `marketing_anti_fold`，已加入后端白名单）；该功能为语义级改写，未接入大模型时明确提示而非生成假结果。
- **今日待办体验课 → 留资管理状态联动**：此前待办里体验课标记「已接待/爽约」只写当日留痕、不回写留资。现补齐业务流转：留资体验课卡片来源直接回写——「已接待」→ 留资状态流转为「已体验」、「爽约」→「爽约」（已成交/已流失等终态不回退），并在对应节次的体验课卡片上写入「已上课/已爽约」结果标记；随心瑜体验预约来源按手机号反查留资，仅「唯一命中一条进行中留资」时联动，多命中视为歧义跳过并在审计留痕（避免把状态误写到重复留资/他人留资上），当日标记不受影响。爽约不清空三次跟进时限（时限仍仅随体验课卡片「已取消」留白，爽约后仍需跟进促改约）；权限复用既有客资处理校验。
- **朋友圈文案自定义字数档位**：「内容长度」在三个预设档位外新增「自定义字数」，选中后出现最少/最多字数输入（10~500 字，默认 60~100），生成时校验「最多 > 最少」并按自定义范围写入提示词。

### 优化
- **朋友圈字数档位下调**：按实际发圈习惯调整为 简短版 30-50 字、标准版 50-120 字、完整版 100-150 字（原 50-80 / 100-180 / 200-300），默认仍为标准版。
- **留资管理体验课结果可视化**：列表「体验课 / 券码」列与编辑弹窗逐节展示「已上课 / 已爽约 / 已取消」标签，待办联动结果一眼可见；弹窗说明补充「已上课/已爽约由今日待办自动回写，不影响跟进时限」。

### 测试
- TodoActionTest 新增 4 个联动用例：留资体验课爽约回写状态与卡片（不影响第 2 节）、已接待流转已体验、终态不回退但卡片照记、随心瑜预约按手机号唯一命中联动/多命中跳过。

## 2026-09-10 ｜ fix: v3.1.46 修复在线升级后全站 500（composer autoload 未重建）

### 修复
- **在线升级后所有接口 500「Call to undefined function ok()」**：v3.1.44 将全局助手函数迁入 `app/Support/helpers.php` 并登记 composer `autoload.files`，但在线升级保留服务器现有 vendor、未重建 autoload 列表；此前生产靠 OPcache 旧字节码侥幸运行，一旦 OPcache 失效重新编译，新控制器调用的 `ok()`/`requireSuper()` 等全部未定义、全站 500。修复三管齐下：
  1. `bootstrap/app.php` 增加 `require_once app/Support/helpers.php` 兜底加载，任何部署路径下助手函数必然可用（双保险，重复加载无副作用）；
  2. `update.sh` 在服务器存在 composer 时执行 `composer dump-autoload`，不存在则提示（由 bootstrap 兜底）；
  3. 生产服务器 vendor 的 `autoload_static.php`/`autoload_files.php` 已补登记 helpers.php 并重启 PHP-FPM，恢复线上服务。

## 2026-09-10 ｜ fix: v3.1.45 修复生产白屏（vendor 循环分包）与在线更新 proc_open 报错

### 修复
- **生产环境整站白屏**：v3.1.44 引入的 `manualChunks` 分包将 vue/vue-router/pinia 与其余依赖拆成 `vue-vendor` 与 `vendor` 两个 chunk，但 `pinia-plugin-persistedstate` 依赖的 `destr`、`deep-pick-omit` 落在 `vendor`，形成 vue-vendor→vendor→vue-vendor 循环引用；浏览器按 ES 模块语义先完整求值 vendor（vue-i18n 的 i18n-t 组件定义在求值时调用 defineAsyncComponent，触发对 vue-vendor 中尚处于 TDZ 的 `isFunction` 的访问），抛 `Uncaught ReferenceError: Cannot access 'O' before initialization`，Vue 应用无法挂载，页面全白。现将 `destr`、`deep-pick-omit` 归入 `vue-vendor` 分包，打破循环依赖。
- **在线更新按钮报「Server Error」**：宝塔 PHP 默认把 `proc_open` 列入 disable_functions，`runShell()` 构造 Symfony Process 时抛未捕获的 `LogicException`，前端只收到笼统的 500「Server Error」；现 `runShell()` 在 proc_open 不可用时返回明确指引（宝塔面板 → PHP 设置 → 禁用函数移除 proc_open），更新接口兜底捕获异常并输出原因。

## 2026-09-10 ｜ feat: v3.1.44 性能与架构专项（索引+分页+缓存+异步同步+控制器化）

### 修复
- **PHP 8.5 配置加载崩溃隐患**：v3.1.43 数据库配置误写裸 `Mysql::ATTR_*` 常量（该类不存在），干净 PHP 8.5 环境（CI、新装机、清空 opcache 后）在加载 `config/database.php` 时直接抛 `Class "Mysql" not found`，本机因 opcache 旧字节码未被暴露；改为 `Pdo\Mysql::ATTR_*`（值不变，仍带回退与弃用告警消除）。
- **测试对本地 `.env` 的隐性依赖**：`KyContractsOverviewTest` 依赖开发者 `.env` 中的 KeepYoga 凭据，凭据清理后 2 个用例失败；改为测试内自含凭据配置，用例完全封闭。
- **五清单徽标计数错误**：会员管理页非「总览」页签下，其余清单徽标恒为 0（只统计当前页签数据）；改由服务端返回全局计数后顺带修复。

### 新增
- **KeepYoga 定时自动同步**：新增 `ky:autosync` 命令并注册每日 05:30 调度（复用既有 `schedule:run` cron，生产零新增配置）；双店增量同步、当天已同步自动跳过（手动同步也算）、与手动导入共用全局锁互斥、SyncJob 批次与「系统定时」审计留痕。
- **全量同步异步化**：`POST /ky/import` 改为受理即返回任务号（生产 FPM 经 `fastcgi_finish_request` 后台执行，无需部署队列 worker），浏览器不再挂起最长 2 小时；新增 `GET /sync-jobs/{job}` 轮询接口，同步页轮询进度、可离开页面、完成自动刷新历史批次；非 FPM 环境保留同步返回全量结果的兼容契约。
- **轻量选项接口**：`GET /customers/options`（会籍顾问下拉去重）、`GET /customers/list-counts`（五清单徽标计数，一次清单扫描+角色范围求交集）。

### 优化
- **数据库索引**：customers（venue/phone/layer/last_visit/expire_date）、leads（phone/venue/status/lead_date/service_teacher）、tasks（owner+status/venue+status/deadline）、training_plans（created_by）、sync_jobs（venue+status+started_at）、ky_cards（member_id）共 16 个索引，迁移幂等可重跑。
- **列表页真服务端分页**：会员/客户页移除 `size:5000` 全量拉取与客户端切片，筛选（清单/评估状态/顾问/姓名/手机号/门店）全部下推；会籍顾问回填下沉到 `/customers` 分页内（当页一次索引查询，替代全量拉 5000 条留资匹配）；顾问下拉改轻量接口。
- **业务缓存**：五清单引擎（`memberListIds`）与经营看板四聚合接口（summary/trends/channels/platforms）按「缓存版本+规则哈希+客户表指纹」缓存，留资/任务/审批/待办/同步写入即时失效，TTL 60~120 秒兜底；`/today/todo`、通知中心等热路径从每次全表 chunk 扫描变为缓存命中。
- **路由层架构**：`routes/api.php` 由 3544 行拆为薄路由 152 行 + `app/Support/helpers.php` 731 行（composer autoload，顺带删除无调用方的 `ArtisanBinary`/`runInRepo`）+ 17 个域控制器，闭包体逐字迁移逻辑零改动。
- **前端分包**：主 chunk 1385KB→约 108KB；element-plus/echarts/vue 供应商独立分包；发现 `v-highlight` 指令零使用但全语言 highlight.js（约 1MB）被打入主包，改为动态 import 按需加载，vendor 1277KB→314KB。
- **登出 PII 清理补全**：新增共享 `clearBusinessPiiStorage()`，登出与会话失效时同时清理 `yimai-sales-store`（谈单门店资料）与 `yimai-training-store:user:*`（按用户键训练计划），共用电脑换账号不再串号。
- **凭据卫生**：`admin-web/.env.local` 清空 KeepYoga 凭据（本地开发代理不再持有真实密码）、`backend/.env` 凭据置空改走后台「登录账号设置」（存数据库）、`.gitignore` 增加 `*.env.bak` 备份形态防护。

## 2026-09-09 ｜ fix: v3.1.43 修复 PHP 8.5 弃用常量污染登录响应

### 修复
- **PHP 8.5 登录报「账号或密码错误」**：数据库连接配置使用已弃用的 `PDO::MYSQL_ATTR_INIT_COMMAND` 常量，PHP 8.5 触发 deprecation 警告并随 `display_errors` 输出到 JSON 响应前，导致前端解析失败、兜底误报密码错误；改用非弃用的 `Pdo\Mysql::ATTR_INIT_COMMAND`（带旧版回退，与 `ATTR_SSL_CA` 同写法）。

## 2026-09-09 ｜ fix: v3.1.42 修复新媒体工作台数据卡片

### 修复
- **新媒体双店数据权限**：修复 v3.1.35 将新媒体错误按单一 `venue` 过滤，导致标准双店账号的运营 KPI、趋势、渠道、平台金额和漏斗全部显示 0/空白的问题；现按账号 `venues` 授权汇总和单店筛选，越权门店由后端拒绝。
- **卡片与图表状态**：成交和核销图使用独立有效数据集，修复标签和值错位、零值显示为横线、0 人漏斗仍显示 100%、局部接口失败沿用旧筛选数据和快速切换时旧响应覆盖新结果的问题，并增加明确加载失败提示。

## 2026-09-09 ｜ fix: v3.1.41 修复生产缺少 finfo 导致同步失败

### 修复
- **随心瑜同步快照**：Flysystem 本地适配器初始化时会无条件实例化 `finfo`，生产 PHP-FPM 未加载 fileinfo 时导致核心导入完成后在快照阶段失败；同步快照改为受控私有目录原生文件读写，彻底移除该运行时依赖，下载继续显式使用 CSV MIME 与完整性校验。

## 2026-09-09 ｜ fix: v3.1.40 安装闭环、同步自愈与低配服务器降载

### 修复
- **生产同步自愈**：确认生产库处于“迁移记录已存在但 `customers.enrolled_at/visit_at` 字段缺失”的半迁移状态；新增幂等修复迁移自动补齐字段，导入失败同步写入系统错误日志，便于后台直接查看原始原因。
- **安装完成闭环**：完成页最后一次明文展示本次设置的登录账号和密码，用户确认保存后用绝对根路径进入 `/#/auth/login`；Vue Router 固定根 base，不再出现 `/install.php#/auth/login`；密码输入不再被前端静默去除首尾空格；安装器增加 no-store、原子 `.env` 写入、配置缓存清理及 SPA 入口完整性检查。
- **2H2G 降载**：会员/卡项分页增加重复整页熔断，避免异常放大到 50 万行；预约和卡项事实相同则跳过数据库更新；双店导入强制串行；CSV 快照改为磁盘 gzip 存储并在下载时流式还原；普通成功请求不再逐条写运行日志，在线更新跳过 vendor 递归权限扫描。

## 2026-09-09 ｜ fix: v3.1.39 修复随心瑜全量导入与全新安装链路

### 修复
- **随心瑜多表全量导入**：修复 v3.1.36 批量预加载按门店过滤全局唯一 `external_id`，导致历史错店会员被重复创建并触发唯一键冲突、整店导入回滚的问题；导入前增加 v3.1.37 新字段迁移检查，数据库未升级时直接给出明确处理提示；前端等待时间与后端 30 分钟同步锁对齐，避免大批量首次同步被浏览器提前判失败。
- **全新安装自动跳转**：首次安装包根路径改为自动进入 `/install.php`，安装成功后再恢复真实 SPA 首页；普通在线升级包不受影响，并在本地与 CI 发行流程加入安装入口、SPA 备份和生产依赖完整性断言。
- **初始超管账号**：首次安装密码下限由 12 位调整为 6 位；创建时显式启用超管，并在写安装锁和删除安装入口前校验密码哈希，避免安装完成后无法登录且无法重试。

## 2026-09-08 ｜ fix: v3.1.38 修复刷新掉登录 + 首页新增留资登记成交卡

### 修复
- **刷新即掉登录**：v3.1.36 锁屏密码不落盘改造时，user store 的 `persist.pick` 白名单漏了 `isLogin`，导致刷新后登录态丢失、被路由守卫踢回登录页。已将 `isLogin` 加回持久化白名单（纯布尔状态无敏感信息），锁屏密码仍不落盘。

### 新增
- **首页「留资登记成交」卡**（老板/店长/老师工作台）：按成交发生日(`deal_at`)、含全部来源统计留资登记的成交人数与成交金额；在「留资管理」改成交金额/标记成交后，首页该卡即时联动。原「线上新客成交率」作为线上漏斗转化率保留，两套口径并存、各自标注数据来源，避免与「随心瑜售卡实收」金额混淆。

## 2026-09-08 ｜ feat: v3.1.37 新客培养栏目

### 新客培养（客户经营新增）
- 新增「新客培养」栏目：统计**入会近 N 天**（默认 90 天，日期可调，含近30/90/半年快捷键）办理正式会员卡的会员，帮助建立练习习惯、了解身体需求、保障体验；
- **新入会判定**：同步 KeepYoga 会员的「入会时间」(create_time_format) 与「到访时间」(visit_time_format) 到本地——有入会时间即正式会员（进本栏目），仅有到访时间为访客（体验卡，不计入）；
- **三分课型 + 三类状态**：私教 / 小班 / 团课分别统计，每类展示 已签到 / 已预约 / 爽约 三项节数；
- **养成进度与健康度**：按各类别目标节数显示进度条，自动判定「待激活」(入会后 0 上课) /「待养成」/「已养成」；顶部概览含新客数、待激活预警、各课型覆盖；
- **预约主题**：聚合新客入会后的上课主题（course_name），辅助了解偏好；
- **养成目标节数可分别调节**：私教/小班/团课阈值并入「会员管理 → 清单规则阈值」弹窗，超管可调；
- **权限与门店隔离**：超管/新媒体看双店，店长看本店，老师只看自己名下会员；
- **数据新鲜度**：顶部标注「数据截至最近同步」，未同步时显式提示而非显示 0。

### 数据层
- `customers` 新增 `enrolled_at`(入会时间)、`visit_at`(到访时间) 字段与同步落库；`setRules` 改为合并写入，避免清单规则与养成阈值互相冲掉；
- 新客培养列表接口 `GET /new-members/cultivation`（一次预载事实聚合，避免逐个会员查询）。

## 2026-09-08 ｜ fix: v3.1.36 安全与数据一致性收尾（审计批次）

延续 v3.1.35 代码审计，完成剩余可安全落地的修复项：

### 安全
- Sanctum 访问令牌默认 14 天过期（`SANCTUM_EXPIRATION` 可配），避免 token 泄露后永久有效；
- AI 大模型接口异常时不再把上游响应体回传前端（仅记录内部日志），既防服务商信息泄露也切断 SSRF 外带通道；
- 前端锁屏密码不再写入 localStorage（仅会话内存），移除随包可逆的固定密钥落盘风险。

### 数据一致性与正确性
- KeepYoga 同步与手工「今日预约」两条快照写路径都改为行锁事务，`fetchedAt` 按门店记录，杜绝并发互相覆盖丢更新；
- 同步会员落库改为一次性预加载 `external_id → 会员` 映射，消除循环内逐条 SELECT 的 N+1；
- 任务流转改进行锁事务，防止并发双推状态；
- 经营看板（趋势/平台金额）的成交、核销日期过滤下推 SQL，不再全历史拉取后内存过滤。

### 性能
- 五清单引擎收敛为「一次扫描算五清单」，工作台首页由 5 次全表扫描降为 1 次，并只 select 必要列；
- 操作日志 `audit_logs` 增加 `(module,target_id)` 与 `time` 索引，默认保留 180 天（超管可调）。

### 分页与前端健壮性
- `/leads` 改为服务端分页，修复全量拉取后内存切片导致的超量数据静默截断；
- 看板加载失败时显示错误提示，不再把故障静默显示为 0 误导经营判断；403 统一给「无权限」文案。

## 2026-09-08 ｜ fix: v3.1.35 安全加固与体验课核销金额小数修复

### 安全与权限加固（全量代码审计后修复）
- **修复新媒体（R_MEDIA）跨店越权**：任务中心、今日汇总、经营看板（趋势/渠道/平台金额）此前新媒体账号可读取两家门店数据，现统一按角色锁定范围——新媒体仅能查看指派给自己的任务与留资漏斗口径，经营看板非超管一律锁定本店（无门店角色返回空）；
- **修复 H5 分享越权覆盖**：`/shares/publish` 增加归属校验，非本人创建的分享仅超管可覆盖，防止劫持他人对外 H5 页面；新增 token 格式与 payload 大小限制；
- **AI 成本滥用防护**：`/ai/chat` 按账号增加每日生成配额（默认 200 次，`AI_DAILY_QUOTA` 可调），配合原有分钟级限流，防止共用付费 key 被单账号刷爆；
- **登录/公开端点限流**：登录、注册与公开 H5（训练计划/销售分享）接口增加速率限制，降低撞库与公开页面被刷风险；
- **SSRF 加固**：AI 接口对外部 baseUrl 的 HTTP 调用关闭重定向跟随，杜绝经 302 跳转绕过内网地址校验；
- **数据一致性**：待办标记处理纳入事务；价格审批改为乐观条件更新防并发双推；配置单例访问加锁，规避并发 firstOrCreate 造出重复行后读到陈旧配置；
- **KeepYoga 同步治理**：增加门店级互斥锁（进行中拒绝重复触发）与超时僵尸任务自动回收，进程被中断不再永久卡「进行中」；
- **更新脚本供应链加固**：`update.sh` 改用固定可信发行地址、强制 HTTPS+TLS1.2、支持 `RELEASE_SHA256` 强校验、禁用降级跳转，降低发行包被投毒风险。

### 功能修复
- **修复体验课核销金额无法保存小数**：成交/核销金额此前被中间件 `integer` 校验拦截，只有整数（如 `.00`）能保存；现改为 `numeric`（最多两位小数），`9.9`、`1280.5` 等均可正常录入与汇总，与数据库 decimal(12,2) 口径一致。

### 前端
- 修复看板/列表「今天」取日使用 UTC 时区导致早间跨天少一天的问题，统一改用本地时区格式化；
- 修复后端异常（无 `code` 字段的 `{ok:false}`/`{errno}` 响应）被前端静默判成功的问题；
- 登出/切换账号时清理含客户手机号等 PII 的持久化业务缓存，避免共用设备串号。

## 2026-09-08 ｜ fix: v3.1.34 版本更新页补齐远端更新记录

- 修复“版本更新”页面只能读取当前服务器已安装包内 `CHANGELOG.md` 的问题：尚未升级时也会读取并展示 GitHub/Gitee 最新发行版的本次更新记录；
- 页面拆分“最新发行更新记录”和“当前已安装版本完整记录”，方便超管在点击更新前确认变更内容；
- 更新日志渲染补齐三级标题，个人中心、KeepYoga、系统日志、老板工作台等分节不再显示成普通正文。

## 2026-09-08 ｜ feat: v3.1.33 个人中心、同步快照与系统日志治理

### 个人中心
- 新增自助昵称、邮箱、手机号设置，真实姓名继续由人员管理维护；保存后立即联动顶栏账号信息；
- 头像支持上传、更换和移除，增加 5MB 客户端限制与服务端图片 data URI 校验；资料接口返回服务端规范化结果，不再在后端模式静默读取其他账号可能残留的浏览器资料。

### KeepYoga 同步
- 修复同步页合同卡恒为 0：原查询 `contract_status=0` 是随心瑜恒空的“待签”口径，现“合同”卡按 `-1` 展示全部合同；未完成签署仍在老板/店长工作台按 `status=1 + 双方签署位` 展示；
- 说明纠正：此前每次同步只读取 JSON 并落库，并不会下载表格，“导出表格”仅是误导文案；从本版起，同步使用本次实际收到的数据生成会员基础、会员卡、团课预约、私教预约 4 份 CSV，保存于服务器私有存储；
- CSV 自动使用日期、门店、数据类型、全量/增量范围命名，历史批次可查看并下载；下载需超管鉴权，文件不进入公开目录和发行包，并增加表格公式注入防护；
- 同步任务改为先创建“进行中”批次，成功、部分失败和异常失败都会留下时间、操作人、范围、错误及快照记录。

### 系统与模型日志
- 系统管理拆分为“系统日志”和“人员操作日志”：系统日志可查看运行/错误日志并按日期、级别、关键词筛选；人员操作日志支持操作人、模块、动作、日期筛选和完整溯源；
- 新增统一保留策略：系统日志默认 7 天，人员操作日志默认永久保留，模型生成记录默认 90 天，均可由超管调整为指定天数或永久保留；新增每日清理命令 `records:prune`；
- 新增服务端模型生成记录，覆盖聊天、朋友圈、小红书、训练计划及本地模板降级，记录操作人、功能、服务商/模型、成功失败、耗时、Token 和安全截断后的结果/错误摘要；超管默认查看最近 10 条并可按操作人、日期、功能、状态筛选；
- 模型配置修改改为服务端操作留痕；技术日志读取保留旧版文件列表/尾部接口兼容能力。

### 老板工作台
- 老板页仅对超管显式加载，未知角色不再误入老板看板；单店视图隐藏无意义的双店对比，并修正访客范围文案；
- “上周”改为上一自然周（周一至周日）；趋势、渠道、今日汇总失败时显示明确错误，合同和随心瑜概览成功恢复后清除旧错误；日期切换不再重复请求无关的实时上游数据。

### 质量
- 新增导入快照与系统记录测试，后端共 57 项 / 305 断言通过；前端类型检查、ESLint 与生产构建通过；新增迁移扩展用户昵称、同步快照、操作日志索引、保留设置和模型生成记录表。

## 2026-09-08 ｜ feat: v3.1.32 留资跟进时限体验课口径 + 合同未签名单可点查

### 留资管理
- **跟进时限改为从体验课起算**：客户 3 次跟进时间（首跟 7 天 / 二跟 15 天 / 三跟 30 天）不再以留资日期为基准，改为从**第一次预约体验课时间**自动计算；
- **体验课卡片新增「已取消」勾选**：若第一次体验课取消，勾选后 3 次跟进时限留空白（表单提示同步更新）；
- **体验课卡片移除按钮始终可用**：即使只剩一节体验课也能删除（恢复"新增一节"空态）；
- **留资客户删除功能**：列表操作列新增「删除」按钮，权限与编辑一致（店长本店 / 超管新媒体全部 / 老师本人或未分配），二次确认后删除并写入操作留痕，新增 `DELETE /leads/{id}` 接口；
- **核销/成交金额支持小数**：`leads` 表 `deal_amount / redeem_amount` 由整型改为 `decimal(12,2)`（新迁移），接口校验 `integer` 改为 `numeric`，分析聚合保留两位小数，修复带小数点金额无法保存的报错。

### 工作台
- **「未完成合同签署」可点击查看名单**：店长/超管工作台该卡片新增「查看未签名单」入口，弹窗列出未签署人员明细（会员姓名 / 合同 / 待会员签署 / 待场馆签署 / 上游状态），后端未签名单不再截断前 20 条。

## 2026-09-07 ｜ feat: v3.1.31 AI 营销工具升级 + 个人中心回归真实账号信息

### AI 营销工具（朋友圈 / 小红书）
- **发圈人设（朋友圈）**：
  - 身份角色精简为 5 个组织岗位：会籍顾问 / 全职老师 / 门店前台 / 管理层 / 主理人（删除「健身教练」）；
  - 「职位」改为「专业」：提供普拉提、瑜伽、康复理疗、减脂塑形、体态调整等专业类别，**支持多选与自定义添加**，与个人中心「专业」共用同一份数据；
  - 品牌固定「一麦」、城市固定「宁波」，不再由用户填写；
  - 客户画像口径丰富：久坐上班族 / 肩颈腰背酸痛 / 体态问题 / 产后恢复 / 失眠焦虑 / 零基础大体重 / 瓶颈健身者 / 银发活力等，支持自定义；
  - **新增「保存人设」按钮**：人设持久化到账号（服务端 users 表），换设备、换浏览器登录不丢失。
- **本条朋友圈设置**：
  - 新增「销售转化」主题分类（体验课邀约 / 续课邀约 / 转介绍有礼 / 限时活动 / 会员日促销 / 价格与价值解读 / 成单感谢 / 满班见证等），目标新增「续课升单铺垫」「沉默客户唤醒」；
  - **首条回复建议**：文案生成后一并输出「评论区首条自评/统一回复」建议，独立展示、一键复制；
  - **历史生成记录**：每次生成的文案与首评自动存档（按账号云端保存、每平台最近 300 条），抽屉内支持载入编辑 / 复制 / 删除。
- **小红书**：按朋友圈同款逻辑适配——账号人设同样可保存到账号、主题分类（含销售转化）选题模板、首评建议（置顶评论）、历史生成记录；品牌/城市写死「一麦 / 宁波」、身份角色与专业与朋友圈/个人中心共用。

### 个人中心（右上角头像进入）
- 从「营销人设维护页」回归真实账号信息：头像上传（canvas 压缩为 128px，保存后**联动顶栏账号菜单头像**与朋友圈预览）、姓名（只读，由超管在人员管理维护）、手机号、性别、年龄、从业年限、专业（多选+自定义，营销工具自动取用）；
- 账号安全：支持自助修改密码，改密后除当前设备外其他设备自动下线。

### 数据与接口
- `users` 表新增 `phone / avatar / profile(json)`；新增 `marketing_posts` 历史表（user_id + platform + title + content + reply + source）；
- 新增 `GET/PUT /my/profile`、`PUT /my/password`、`GET/POST/DELETE /marketing/history`；`/me` 同步返回 phone / avatar；
- 生成入口改为一次调用输出 `{正文, 首评}` 结构化 JSON，并自动落历史（失败静默、演示模式回退 localStorage）；
- 新增 `MyProfileMarketingTest`（资料/人设存取、改密校验、历史按账号与平台隔离、非法平台拒绝），后端测试增至 52 项 / 268 断言。

## 2026-09-07 ｜ feat: v3.1.30 今日预约手机号回填会员档案

- 今日预约行 KY 未带手机号时，自动回填匹配到的会员档案手机号，保证「今日待办」全量手机号展示可用。

## 2026-09-07 ｜ fix: v3.1.29 更新脚本平滑重载 PHP-FPM（修复新增类短暂 500）

- 根因复盘：v3.1.25 上线后「今日待办」500（`Class App\Models\TodoAction not found`）——生产 PHP-FPM 长驻进程持有旧 OPcache/自动加载状态，新增模型类在后续请求中一度无法加载；后续更新触发 rsync+清缓存后自愈。
- `update.sh` 在收尾新增 PHP-FPM 平滑重载（按 php-fpm-84/83/82/php8.4-fpm 等常见服务名逐一尝试，未匹配则跳过），确保每次更新后新增类、路由立即生效，不再依赖进程自然回收。

## 2026-09-07 ｜ feat: v3.1.28 日志接口支持按天日志与目录列表

- `GET /system/logs` 支持 `?list=1` 列出 storage/logs 下全部日志文件、`?file=` 读取指定文件尾部（默认 laravel.log，兼容 LOG_CHANNEL=daily 的 laravel-YYYY-MM-DD.log）。

## 2026-09-07 ｜ feat: v3.1.27 新增超管远程日志查看接口（排障用）

- 新增 `GET /system/logs`（仅超管）：返回 Laravel 当日日志尾部（最后 64KB），远程定位线上 500 无需 SSH/面板，配合版本更新完成自服务排障闭环。

## 2026-09-07 ｜ fix: v3.1.26 修复线上今日待办接口 500（迁移半成功状态）

- 排查结论：生产环境升级到 v3.1.25 后「今日待办」全为 0，根因是迁移 `2026_09_06_000003` 在生产库**半成功**——MySQL DDL 不带事务，`customers.cards_list` 加列成功、`todo_actions` 建表失败（未标记完成），导致 `/today/todo` 查询留痕表时 500，前端静默显示全 0。
- 迁移改为幂等可重试：`cards_list` 加列与 `todo_actions` 建表均先检查是否已存在；`todo_key` 缩短为 80 字符（utf8mb4 唯一索引约 323 字节，兼容未开启 innodb_large_prefix 的 MySQL 5.6/5.7 旧配置 767 字节上限）；重跑迁移即可完成剩余建表。
- 前端今日待办加载失败时显式报错（ElMessage），不再静默显示全 0 造成误导。
- 更新步骤：页面点「版本更新」→ 升级脚本自动补跑迁移 → 今日待办恢复；无需人工操作数据库。

## 2026-09-06 ｜ feat: v3.1.25 今日待办升级为可执行闭环（工作台概要 + 客户经营完整页）

- 今日待办拆分为两级：**工作台只显示概要**（七分组待处理数 + 前 2 条姓名样例 + 已处理数，点击任一分组直达对应清单）；**客户经营菜单新增「今日待办」页**（/yimai/todo）承载完整列表与逐项操作，URL 支持 ?group= 直达分组。
- 待办可操作、可流转：每条待办带「处理」动作菜单并写入新的 `todo_actions` 留痕表（当天全店共享消隐、记录处理人/动作/备注，可展开查看）——
  - 新客首响：**已首响 → 留资状态自动流转为「已联系」**、无效客资 → 已流失（带处理权限校验：仅超管/店长/该客资服务老师）；
  - 待续费/流失风险：标记沟通自动更新会员「最近触达」（last_touch），支撑待复活 2 周有效触达口径；「登记续课计划」「登记下降处置」在页内直接打开表单（与会员管理同接口），保存即生成清单工作流字段并标记已处理；「转待复活」一键流转；
  - 生日关怀/体验课/今日预约：标记已送祝福、已接待等动作留痕；今日任务保持跳转任务中心走既有状态机。
- 今日待办全量显示手机号（此前仅尾号），待续费/流失风险/生日/体验/预约各行均带完整号码。
- 会员管理「剩余课时」列升级：合计节数下方以小字**逐卡列出全部有效卡项**（名称 + 剩余节数/天数 + 未开卡标记，悬浮显示完整清单），时间卡以「剩余天数」呈现——纯时间卡会员剩余课时显示「—」但卡项明细可见。
- 新增 `TodoActionTest`（标记流转留资状态、触达更新、越权拒绝、重复标记合并），后端测试增至 47 项 / 239 断言。

## 2026-09-06 ｜ feat: v3.1.24 待续费多卡口径与阈值拆分（次卡节数/占比、到期天数/有效期占比）

- 修复多卡会员待续费误报：原同步汇总只取「主卡」一张卡算剩余节数与到期日，一张用完/没余额的卡排到主卡位就把整个会员标成待续费。现改为全部有效卡汇总（`card_stats` 新字段）：次卡剩余节数 = 所有有效次卡合计（**含未开卡**，未开卡的课时计入库存）；主卡仅作展示。
- 会员管理剩余课时口径升级：`remain_times` 从「主卡剩余」变为「全部有效次卡合计（含未开卡）」；时间卡会员不再被误判为 0 节（无次卡时该值为空而非 0）。
- `expire_date` 从「主卡到期日」改为「全部有效卡中最早的到期日」，续费/到期提醒不再被晚到期的主卡掩盖。
- 待续费阈值拆分为四项（会员管理「调整标签阈值」可调，实时联动今日待办）：
  - **次卡剩余(节)**：全部次卡合计剩余 ≤ 阈值且最近月有出勤（原待续课阈值，默认 10）；
  - **次卡剩余占比(%)**：合计剩余 ÷ 合计绑定(剩余+已用) ≤ 阈值（默认 20，捕捉 200 节大卡用到尾段；0=关闭）；
  - **到期提醒(天)**：任一有效卡剩余天数 ≤ 阈值（默认 30，原为硬编码）；
  - **有效期占比(%)**：时间卡剩余天数 ÷ 有效期天数 ≤ 阈值（默认 0=关闭，数据可用后可开启）。
- 多卡口径说明：已用完（剩余 0）的卡不再单独触发提醒——会员还有其他有效卡库存时不标待续费；纯时间卡会员只在到期临近时提醒，不再因「0 节」误标。
- 30 天续费评估的续费窗口计分同步改用可调阈值（原硬编码 10/20/30/60），与清单口径一致。
- 前端本地演示规则缺新键时自动合并默认值；阈值弹窗新增四项并说明多卡口径。
- 新增 `CardStatsRenewalTest` 覆盖：废卡+健康卡不误报、大卡尾段占比命中、未开卡库存不提醒、纯时间卡按到期天数判定、有效期占比规则、旧数据回退、阈值接口。后端测试增至 43 项 / 219 断言。

## 2026-09-06 ｜ feat: v3.1.23 合同签署口径修复、随心瑜经营概览与今日待办阈值同步

- 修复工作台「未完成合同签署」数量恒为 0 的根因（依据《随心瑜后台完整解读》实测口径）：原实现用 `contract_status=0`（待签，上游恒为空）且签署位字段名解析错误（找的是 `*_sign_status`，实际为 `customer/venue_signatory_status`，0=未签、2=已签）。现改为按 `status=1`（签署中）+ 双方签署位判断，并与绿地店实测对齐（签署中 5：待会员签 4、待场馆签 1）。
- 合同卡新增「已过期」提醒（`status=5`，发起后顾客一直未签；绿地 116 / 东部 43），超 0 红色显示；单店读取失败不再整卡报错，按门店显示错误说明。
- 新增 `GET /ky/overview` 经营概览接口：直读随心瑜数据分析三件套（`getvenuedataoverview` 收入/耗卡/剩余资产、`getmembershipactivityanalysis` 活跃/风险/沉寂/流失、`getvisitorconversion` 访客转化），`KyClient` 白名单同步加入三个接口；超管/店长工作台新增「随心瑜经营概览」卡片（双店按门店范围汇总，分析不可用时显示 — 而非误导性 0）。
- 今日待办阈值与客户管理实时同步：`/today/todo` 返回当前生效阈值，待办卡头部新增阈值行「待续课 剩余 ≤N 节 · 预流失 N-N 天未到店 · 待复活 >N 天 · VIP 实收 ≥N 元」，刷新时先拉最新规则再聚合，在会员管理调整阈值后今日待办立即按新口径重算。
- 新增 `KyContractsOverviewTest` 回归测试（合同签署位解析/过期数/门店隔离、经营概览聚合与角色守卫、阈值同步返回），后端测试增至 35 项 / 194 断言。

## 2026-09-06 ｜ feat: v3.1.22 工作台新增「今日待办」栏目

- 工作台新增「今日待办」栏目（新组件 `today-todo.vue`），按分组页签汇总当天需要服务的客户：今日预约 / 待续费 / 流失风险 / 生日关怀 / 体验课 / 新客首响 / 今日任务，每组显示数量徽标、默认展示前 8 条可展开全部，默认落在第一个有待办的分组。
- 新增后端聚合接口 `GET /today/todo`：一次返回今日预约明细（随心瑜 `ky_bookings` 事实，含时间/课程/课型/老师/签到状态，course_type 2=私教、3=小班、1=团课）、待续费（待续课清单，临近到期优先并标注 7 天内到期）、流失风险（预流失+待复活+出勤降低，标注最长未到店天数）、生日关怀（今天与未来 7 天生日）、体验课（随心瑜体验预约事实 + 留资体验课卡片双来源）、新客首响（超 24 小时未首响标红）、今日到期或逾期任务。
- 今日预约逐条匹配会员档案（手机号 → 随心瑜会员 ID → 姓名+门店），行内直接标注服务提示：五清单命中（待续课/预流失/待复活/出勤降低/VIP）、评估低分、需协助、7 天内到期、今日生日，让店长/老师看到"今天该对这个会员说什么"。
- 角色隔离沿用工作台口径：超管看双店、店长/老师限本店（老师另限本人会员明细）、新媒体仅展示新客首响与体验课；预约列表按门店展示全店预约。
- 新增会员 `birthday` 生日字段（`customers` 表迁移 + KeepYoga 同步多候选字段提取，仅上游有值时覆盖，不冲掉人工维护数据）；会员管理新增「生日」列（今天/7 天内生日高亮）与「生日」维护弹窗，保存后进入今日待办生日关怀。
- 前端 `getTodayTodo()` 新增（后端模式 + 本地演示模式分支），`YimaiCustomer` 类型补 `birthday` 字段。
- 新增 `TodayTodoTest` 回归测试（聚合分组、门店隔离与超管双店、新媒体范围、生日维护接口），后端测试增至 30 项 / 160 断言。

## 2026-09-05 ｜ fix: v3.1.21 修复店长工作台三分显示为 0 的误导

- 修复「随心瑜今日预约」卡在旧快照缺少 `todayKinds` 时错误显示「私教 0 · 小班 0 · 团课 0」：`/today/summary` 在快照无 `todayKinds` 时返回 null，前端今日预约卡改为中性提示「团课 + 私教预约记录（更新快照后显示细分）」，避免数字与三分不一致。
- 约课人数 / 上课班次 KPI 卡：当所选时段确无预约或签到数据时，小字由「私教 0 · 小班 0 · 团课 0」改为「本时段暂无预约数据 / 签到班次数据，可在 KeepYoga 同步后查看」，说明三分口径已生效但暂无数据的原因。
- 三分课型口径沿用 v3.1.20：`course_type` 2=私教、3=小班（精品课）、1=团课（精品团课）；老板与店长工作台一致。
- 后端测试保持 26 项 / 123 断言。

## 2026-09-02 ｜ feat: v3.1.20 门店经营预约与班次按私教/小班/团课三分

- 门店经营「随心瑜今日预约」「约课人数」「上课班次」卡片下方小字由「团课+私教」两分改为「私教 · 小班 · 团课」三分展示。
- 课型依据随心瑜课程类型判定：`course_type=2` 私教、`course_type=3` 小班（随心瑜界面显示为「精品课」）、`course_type=1` 团课（「精品团课」）。
- 今日预约快照落库新增 `todayKinds`（私教/小班/团课三类计数），工作台按门店或双店汇总展示。
- 后端 `analytics/trends` 预约/上课班次拆分从两分扩展为三分（`smallBookingCount`/`smallClassCount` 等新字段），上课班次去重键同时区分课型，避免小班与团课在同一时间/老师/课程被合并。
- 前端 `TodaySummary`/`DashboardSummary` 类型与映射同步新增小班字段。
- 更新相关回归测试，后端测试保持 26 项 / 123 断言。

## 2026-09-02 ｜ feat: v3.1.19 人员管理完善与门店经营预约拆分

- 人员管理「开通新账号」补齐「姓名」字段：姓名与登录名分开填写，姓名默认回退为登录名，列表「姓名」列真实展示。
- 人员管理新增「删除账号」操作：删除同时撤销该账号全部登录态，已注销账号无法再登录；本人账号仍禁止停用/删除/重置。
- 门店经营「约课人数」「上课班次」卡片下方小字新增按私教 / 团课·小班的拆分展示，便于区分两类课程经营情况。
- 售卡张数提示文案同步为「随心瑜会员卡实收口径（非财务流水）」，与售卡金额口径一致。
- 新增「姓名创建 + 删除账号」「预约/班次按类型拆分」回归测试，后端测试增至 26 项 / 121 断言。

## 2026-09-02 ｜ feat: v3.1.18 门店经营接入随心瑜真实售卡数据

- 新增 `ky_cards` 售卡事实表：全量导入时将会员卡表按上游卡 ID 幂等落库，保存卡项名、实收金额、标价、售卡时间、卡状态、是否为体验卡、会籍顾问和会员信息。
- 门店经营「售卡张数」不再依赖工作台留资手动登记的成交卡项，改为按 `ky_cards` 售卡时间与实收金额统计，排除体验卡（`is_taste=1`）和退卡，与随心瑜会员卡表口径对齐。
- 售卡金额提示文案同步更新为「随心瑜会员卡实收口径（非财务流水）」，「登记售卡金额趋势」标题改为「售卡金额趋势」。
- 新增售卡分析测试用例，确认体验卡与退卡被正确排除、不同门店独立统计；后端测试保持 25 项 / 112 断言。

## 2026-08-30 ｜ feat: v3.1.17 第一阶段安全与权限加固

- AI 大模型代理安全封堵：普通角色聊天请求不再携带或决定 `baseUrl/apiKey/model`，统一使用超管保存的服务端配置；模型拉取与连接测试仅超管可用；所有外发地址校验为公网 HTTPS 且拒绝内网/回环/保留地址（防 SSRF 与密钥外泄）；对消息数量和总长度、流式与接口频率限流。
- 留资管理补齐对象级与字段级权限：店长只能操作本店并强制本店创建/更新；老师仅本店且本人负责或待认领，只能跟进字段、只能指派给自己、不能标记成交；超管与新媒体保持双店管理；`/leads/check`、详情、历史、新增、编辑全部走统一权限，并补全字段校验。
- 人员安全：停用账号或重置密码时立即撤销该账号全部 Sanctum Token；旧 Token 访问统一被 403 拒绝；登录与账号管理维持既有角色限制。
- 工作台与通知数据隔离：`/today/followups`、`/today/alerts` 按角色门店统一使用客户/留资范围函数，新媒体不再收到会员与任务告警，老师只看到本人/本店客资。
- 前端会话清理：401 统一触发完整登出——清除用户信息、动态路由、菜单、工作标签、训练计划等敏感状态，避免账号切换或会话过期后串号、串菜单。
- 训练计划跨账号隔离：本地存储按用户 ID 分区，账号切换/退出即时清空，云端空列表也强制覆盖，演示种子不再混入生产数据。
- 审计日志改为真正服务端分页，超过 20 条不再截断，筛选会重置页码。
- 价格审批：前端老板角色统一为 `R_SUPER`（原错误使用 `R_BOSS`），店长终审阶段只显示等待、关联成交仅超管可见；后端补状态筛选；超管创建审批可指定门店。
- 首次安装与运行环境统一要求 PHP 8.4.1+：安装器检测、Composer 平台约束、DEPLOY 与 README 同步更新，避免 PHP 8.2/8.3 环境检测通过后 vendor 直接 Fatal。
- 新增安全与权限回归测试（`AiSecurityTest`、`LeadAuthorizationTest`）：服务端 Key 只发往已保存公网地址、私网目标被拒、非超管不能探测模型、四角色×双店留资矩阵、停用撤销 Token、工作台范围；后端测试增至 25 项 / 112 断言。

## 2026-08-30 ｜ deploy: v3.1.16 宝塔首次安装包进入自动发行流程

- GitHub Actions 和本地 `make-release.sh` 同时构建两类包：普通在线升级包继续保留现有依赖与配置，新增 `yimai-workbench-installer-v<版本>.zip` 用于全新服务器首次安装。
- 首次安装包内置 PHP 生产依赖 `vendor`、已构建 Vue 静态资源、Web 安装向导和 `update.sh`；新服务器无需 Composer、Node.js 和手工创建超管。
- 安装向导完成 PHP/扩展/目录权限/vendor 检查，支持面板预建数据库或自动建库，自动生成 APP_KEY 与 `.env`、执行全部迁移、创建双店超管，并可选写入 KeepYoga 凭据。
- 加固 Web 安装入口：数据库名/端口/管理员账号验证，`.env` 特殊字符安全转义，安装完成生成 `storage/install.lock` 并自动删除 `public/install.php`。
- `update.sh` 改为自动以自身目录作为站点根，不再写死生产域名目录；普通升级包同步携带最新版更新脚本，新装后可直接使用后台在线升级。
- 部署文档明确首次安装包与在线升级包的用途、目录结构、站点运行目录、安装步骤及安全边界。
- 使用独立临时数据库完成端到端安装验证：特殊字符数据库密码、18 个迁移、超管创建、锁文件和安装入口自删除全部通过。

## 2026-08-30 ｜ fix: v3.1.15 留资编辑修复、AI品牌头像与全量稳定性审查

- 工作台右上角「一麦AI助手」的机器人头像替换为一麦绿色 Logo，问候消息和后续 AI 回复统一品牌形象。
- 修复编辑留资必现 500：`camel()` 返回数组，原代码错误调用 `->toJson()`；改为正确 JSON 编码，并通过完整编辑载荷回归测试验证。
- 留资新增/编辑写入进一步加固：只允许真实库表字段；校验 camelCase 的日期与金额字段；缺少留资日期自动补当天；空金额归一为 `null`，避免 MySQL 严格模式报错。
- 会员工作流日期与布尔值规范化：空日期安全存为 `null`；兼容 `false/true/0/1` 字符串布尔，避免 PHP 把字符串 `"false"` 误存为真。
- 审批权限和状态机加固：老师/新媒体不能审批，店长只能操作本店且只能完成店长初审，老板负责终审；禁止跨店、越级、重复审批；审批单新增门店字段并按申请人门店回填历史数据。
- 工作台待跟进统计排除空的下一步时间，并完整包含次日截止任务；店长价格审批列表、KPI 与通知统一按本店隔离。
- 留资、会员、任务、审批的列表加载和保存补齐失败提示，接口异常不再静默；看板多接口改为独立容错，单个接口失败不再拖垮整页。
- API 返回增加空数组/空计数归一化；平台金额、手机号命中、留资分页、今日预约空值和双店数值求和补齐防御性处理。
- 修复版本更新页本地/远端提交判断方向错误，避免本地较新时误报可更新。
- 新增留资创建、留资编辑、跨店审批权限、会员布尔值归一化等回归测试；后端测试增至 12 项 / 50 断言。

## 2026-08-30 ｜ feat: v3.1.14 会员标签金额口径、留资保存修复与店长数据隔离

- 会员管理「累计购买」改为「累计购买金额」：按随心瑜会员卡 `deal_price`（实收金额）累计，排除体验/员工/测试/赠品与退卡卡项，多张卡相加；VIP 标准同步改为「累计购买金额 ≥ 3万」（新增 `vipAmountThreshold`，超管阈值弹窗改为 VIP阈值(元)）。
- 修复新媒体留资「点保存无反应」：新增/编辑留资时前端把 `phone_tail`（非库表字段）与 `id` 一并提交，被 Laravel 全字段批量写入导致 `Unknown column` 500，前端又无错误提示，表现为保存没反应。后端 `/leads` 写入改为字段白名单，只落库留资真实字段；前端保存失败弹具体错误提示，不再静默。
- 体验课「上课时间」框优化易用性：补占位文案、可清除、带日历图标，一眼可点。
- 店长端数据隔离：会员管理/客户经营池/任务中心隐藏「门店」筛选，店长只显示本店数据（后端本就按店长门店 scope，前端同步隐藏筛选入口）。
- 「调整续费/流失阈值」按钮文案改为「调整标签阈值」，与五清单标签口径一致。

## 2026-08-30 ｜ feat: v3.1.13 门店经营口径、合同签署与角色通知

- 网站 favicon 全部替换为一麦绿色 Logo，前端公共资源和 Laravel 直达图标保持一致。
- 老板/店长工作台“门店经营”改为约课人数、上课班次、售卡张数、售卡金额；上课班次按随心瑜已签到的课程/老师/时间去重，售卡按工作台已成交且登记卡项统计并明确非财务实收。
- 成交率移入新媒体运营并改为“线上新客成交率”，仅统计美团、大众点评、抖音、小红书、视频号、线上/团购来源在所选周期内的新客留资成交比例。
- 新增成交与核销事件时间，成交金额、核销金额按业务发生时间聚合；历史无事件时间数据兼容回退留资日期，本月趋势补齐全部零数据日期。
- 老板与店长工作台新增未完成合同签署卡片，区分待会员签署、待场馆签署和状态未知；仅明确状态参与未签统计，未知字段不误报。
- 会员管理按五清单重排重点列：续费评估仅在待续课显示，总览精简，出勤降低/VIP/预流失/待复活分别突出对应处置字段和下一步动作；切换页签自动清除总览遗留筛选。
- 通知中心从模板静态数据改为真实服务端数据，支持打开刷新、手动刷新、单条点击、单条已读、全部已读和真实未读红点。
- 通知按角色和数据范围分级：超管看双店同步/审批/任务，店长看本店，老师看本人任务及会员，新媒体看本人客资；已读状态跨设备保存。

## 2026-08-29 ｜ feat: v3.1.12 续费经营评估与角色任务闭环

- 会员管理新增“续费评估”和“下一步动作”常驻列：直接展示最近分数、经营等级、评估日期、评估人、是否过期，以及动作负责人、截止时间和逾期状态。
- 评估筛选新增待评估、高机会、重点培育、风险修复、已过期，便于店长和老师快速定位需处理会员。
- “朋友圈互动”替换为“企业微信有效沟通与客户回应”；评估维度统一改为互斥单选，避免旧版复选重复加分，风险项单独多选扣分。
- 评估改为固定100分：最近30天随心瑜签到、剩余课时和到期窗口由系统自动计分；人工只判断训练进展、课后反馈、企微沟通、续费意向、服务处理和风险。
- 新增续费评估历史表，保存完整答案、分数、等级、评估备注、评估人与时间；客户端无法伪造总分，由服务端统一重算。
- 保存评估自动生成或更新客户关联任务，并回写会员下一步动作：高机会确认续费方案，重点培育跟进障碍，低分风险由同店店长介入；任务支持负责人、截止时间和店长验收。
- 角色权限统一：老师仅查看和操作本人负责或本人担任会籍顾问的会员，店长仅本店，超管双店；新媒体禁止评估会员，全局清单阈值仅超管可修改。
- 任务接口补齐门店/状态筛选与后端状态机，老师只能认领和提报本人任务，只有店长及以上可验收，不能再依赖前端按钮隐藏保障权限。
- 修复随心瑜增量同步可能把三个完整自然月出勤覆盖为零：每次同步后统一从预约事实表重算 M1/M2/M3 和最近到店。

## 2026-08-29 ｜ feat: v3.1.11 随心瑜预约事实接入与双店看板修复

- 修复 KeepYoga 生产代理路径与后端白名单格式不一致的问题；实时会员、访客、会员卡、合同计数失败时展示真实错误，不再统一误报“上游接口未返回计数”。
- 今日预约改为本地日期查询，合并团课与私教，支持分页、重复页检测和预约去重；排除取消、爽约、作废、未到记录，并从会员名、课程名、卡名和备注综合识别体验预约。
- 今日预约任一门店获取失败时禁止以零覆盖旧快照；老板与店长工作台新增“随心瑜今日预约 / 今日体验预约”指标及快照时间。
- 新增 `ky_bookings` 预约事实表：全量同步幂等保存近两年团课/私教预约、门店、会员、课程、老师、状态和体验标识，后续按三天回溯增量更新。
- 工作台绿地店、东部店和双店汇总改为后端独立筛选；约课按有效体验预约去重人数补足，体验/到店按已签到或已完成人数统计，取消记录不计入。
- 留资经营统计按业务日期 `lead_date` 归属，不再错误使用记录创建时间；修复到店人数直接等于约课人数及转化率可能超过 100% 的口径问题。
- 上线升级脚本会自动执行数据库迁移；升级完成后需由超管进入“KeepYoga同步”，执行一次“全量导入会员到客户池”完成预约事实首次回补。

## 2026-08-28 ｜ feat: v3.1.10 留资新增弹窗体验课卡片化

- 留资「新增/编辑」弹窗重构：把主表单中的 体验课时间、体验课主题、上课老师、券名称、券码总次数/剩余次数、平台券码、核销金额 全部并入「体验课」卡片（原「体验课券码」），每点一次「新增一节」录入一节完整体验课。
- 每一节体验课独立记录：上课时间、主题、上课老师、下单平台、券名称、券码、总次数、剩余次数、核销金额；主表单保留 下单平台（驱动平台统计）与 成交卡项/成交金额。
- 核销金额按节录入，保存时自动汇总到 `redeem_amount`，经营看板各平台核销/成交统计口径不变；列表「体验课 / 券码」列按节展示时间/主题/老师/券信息，旧单节数据兼容显示。
- 历史遗留的单节字段（trialTime/trialTopic/trialTeacher/voucherCode）改为可选，仅兼容旧数据。

## 2026-08-28 ｜ feat: v3.1.9 人员/审计/搜索/同步完善

- 人员管理：新增「编辑」能力，可直接修改账号角色与门店范围（超管/新媒体自动双店）；新增账号、停用、启用、重置密码沿用既有流程。
- 自助注册：修复注册成功后未真正建立登录态（补 `setToken/setLoginStatus`），跳转落地页修正为 `/yimai/today`。
- 客户经营池：搜索框拆分为「客户姓名」与「手机号 / 尾号」两个独立条件；后端 `/customers` 拆分 name / phone 过滤。
- 会员管理：新增「手机号 / 尾号」独立搜索框，与会员姓名分开筛选。
- 留资管理：搜索已拆分姓名与联系方式；「数据范围」提示随所选门店筛选动态更新（如选择绿地店显示「数据范围：绿地店」）。
- 操作留痕：清除「阶段1待补充」占位文案；后端审计记录新增操作 IP 与设备信息（User-Agent 提炼 OS/浏览器/端型），溯源抽屉展示；模块筛选补全会员/任务/训练/同步/人员/模型等全部来源。
- KeepYoga 同步：实时计数失败时在卡片下方展示具体错误原因，连接失败区分「未配置随心瑜登录账号」；今日预约解析兼容上游多种返回结构（reservations/list/rows）。
- 历史同步批次：新增「同步明细」列，展示导出表格（会员基础表/会员卡表/团课预约/私教预约）与导入落库统计（新增/更新/未变化/跳过）及出勤月份。

## 2026-08-28 ｜ feat: v3.1.8 留资平台/体验课卡片 + 新媒体仪表盘 + 权限修复

- 修复个人中心空白 404：`个人中心` 路由不再挂在「系统管理（仅超管）」分组下，改为全角色可访问的隐藏直达路由；右上角头像菜单「个人中心」改跳转 `/yimai/profile`。
- 修复大模型仅超管可用：`GET /ai/config` 原限制超管，导致新媒体/店长/老师水合配置失败并提示「尚未接入大模型」；改为全部登录角色可读取配置（不含密钥明文），PUT 保存仍仅超管。
- 新增留资增强字段：下单平台、微信号、券名称、券码总次数/剩余次数、体验课节次卡片（`trial_cards`，支持「第一节课-券码、第二节课-券码」逐节录入）。
- 新增留资弹窗「体验课券码」卡片：点击「新增一节」逐节添加，每节含券名称/下单平台/券码/总次数/剩余次数；列表展示券码剩余次数与体验课节数汇总。
- 留资管理查询：新增「联系方式/手机号/微信」搜索框与「留资日期」范围筛选（后端 `/leads` 支持 phone / dateFrom / dateTo）。
- 留资状态新增「爽约（已约未到）」；新增留资时输入手机号自动命中检测（`GET /leads/check`），命中会员/已有留资即时提示，避免重复录入。
- 新媒体仪表盘：新增「本月成交金额」「本月核销金额」KPI；新增「各平台成交金额」矩形树状图与「各平台核销金额」柱状图（`GET /analytics/platforms`，按下单平台聚合，缺失时回退来源渠道）。

## 2026-08-28 ｜ feat: v3.1.7 会员/工作台真实数据 + AI流式修复 + 增量导入

- 会员管理：门店筛选对所有清单页签生效；表格新增「门店」列；「数据范围」文字随门店/清单/顾问筛选动态显示。
- 阈值设置弹窗扩充：五清单规则（待续课节数、VIP节数、出勤下降口径、预流失天数、待复活天数）全部可自定义调整，前后端同步实时重算。
- AI 助手：修复流式回复不实时显示的问题（响应式更新）；回复做排版（加粗/列表/换行）；SSE 解析兼容 `\r\n` 与无结束标记。
- 新媒体：无权限子菜单的父级（如空的「业务执行」）不再显示，修复窗口重叠/空内容。
- 工作台/经营看板改为真实数据：成交金额、成交人数、约客人数、留资、核销、会员总量均来自留资/会员表；渠道分布为真实来源统计；新媒体看板从留资列表取数。
- KeepYoga 导入改为**增量同步**：出勤只拉取上次同步以来的区间（首次才拉近两年），未到访会员保留历史最近到店；大幅减轻重复全量导入压力。

## 2026-08-28 ｜ feat: v3.1.6 留资字段调整 + AI流式 + 表格滚动修复

- 留资管理「客户分级」改为「体验课类型」（定制私教/私教小班/精品团课/其他）。
- 会籍顾问改为下拉选择（可从在册会员会籍顾问中选择/手输），超管可改、新媒体端同样显示该字段。
- 修复客户经营三页（会员管理/留资/经营池）表格底部内容被裁切、无法滚动的问题。
- AI 助手与营销工具改为**流式响应**（后端 `/ai/chat` 支持 stream，前端 SSE 逐字输出）：中转站等对 stream 请求才结算 token，解决「同步模式不回传用量」导致的无法回复。

## 2026-08-28 ｜ feat: v3.1.5 会员数据洗板修复 + 门店筛选

- 修复「累计购买」恒为 0：随心瑜卡表 `initial_amount` 是「N次」字符串且含义为赠送次数，改用私教**次数卡**的 `剩余节数 + 已用节数` 作为累计购买私教课量，VIP 清单、会员管理累计购买列随之生效。
- 修复全量导入内存溢出：`KyMemberSyncService::sync()` 放宽 `memory_limit=512M` 并 `set_time_limit(0)`，两年预约流水不再 OOM。
- 会员管理新增「门店」筛选（绿地店/东部店），客户经营栏目三页均支持门店筛选。

## 2026-08-28 ｜ fix: v3.1.4 升级脚本兼容宝塔锁定文件

- `update.sh` 收尾属主整理对 `.user.ini` 等被宝塔 `chattr +i` 锁定的文件容错（storage/bootstrap-cache 仍严格归主），修复「更新脚本执行失败 chown: Operation not permitted」。
- 站点文件属主统一为 `www:www`，后台按钮(www)与计划任务(root)均可正常覆盖升级。

## 2026-08-28 ｜ feat: v3.1.3 在线升级可用 + 更新日志/文档随版本同步

- 修复后台「版本更新 → 立即更新」报 500：生产 PHP 的 `disable_functions` 禁用了 `proc_open`，Symfony Process 无法拉起更新脚本，已解除该限制（详见 DEPLOY.md 部署前置说明）。
- 修复升级 rsync 权限失败：站点文件属主统一为 `www:www`；`update.sh` 更新完成后整体 `chown -R www:www` 并给 storage/bootstrap-cache 加写权限，保证后台按钮(www)与计划任务(root)后续都能覆盖写入。
- 更新日志同步：构建产物把 `CHANGELOG.md` 一并放入 `backend/`，每次升级完成后「版本更新」页日志与版本保持一致；`make-release.sh` 与 GitHub Actions 同步调整。
- 文档留痕：README 新增「版本记录」表，功能模块与当前实现对齐；DEPLOY.md 补充在线升级按钮的 PHP/open_basedir 前置要求。
- 版本号同步更新（`VITE_VERSION` 3.1.3）。

## 2026-08-28 ｜ feat: v3.1.2 会籍顾问统一口径与匹配

- 术语全系统统一为「会籍顾问」：会员管理、客户经营池、留资管理、同步页面的「顾问/服务老师/销售顾问」全部统一命名。
- 会员管理「会员」列与客户经营池保持一致：展示 `手机号 · 来源`；「会籍顾问」独立成列（待分配红色标签）。
- 新增会籍顾问匹配：会员档案会籍顾问为空时，自动按手机号匹配留资里的服务老师，减少「待分配」误报（会员管理、客户经营池同步生效）。
- 会籍顾问筛选下拉与新增匹配数据联动。

## 2026-08-28 ｜ feat: v3.1.1 会员管理顾问筛选

- 会员管理「顾问」从会员列拆出为独立列（未分配显示红色「待分配」标签）。
- 新增「顾问」筛选下拉：可查看任意顾问名下会员，含「待分配」筛选项。
- 后端 `/customers` 支持 `consultant` 过滤参数（服务端 + 演示模式双通道）。

## 2026-08-28 ｜ feat: v3.1.0 补全未完成功能 + AI配置存库 + 在线更新加固

- AI 配置改为服务端数据库保管（`app_settings.ai`），任意设备登录自动水合使用同一份配置；AI 配置页改为展示“服务端保管”提示。
- 修复首页「一麦AI助手」永远提示未接入的问题：启动/调用前统一从服务端水合 AI 配置。
- 版本更新：后端返回完整更新脚本输出、前端展示失败明细面板；`update.sh` 加强健壮性（无 python3 兜底解析、双源重试下载、更清晰报错）。
- 任务中心：新增「新建任务」，支持认领开始执行、分配负责人、提报完成、店长验收通过/退回（后端 `POST/PATCH /tasks`）。
- 客户360：客户经营池新增详情抽屉（主档台账 + 工作流留痕时间线 + 按手机号关联的留资），新增 `GET /customers/{id}`。
- 价格审批：新增「发起价格审批」入口，终审通过后可「关联成交」（后端 `POST /approvals` + decide 支持关联成交）。
- 人员管理：新增「开通新账号」「停用/启用」「重置密码」，支持角色与门店范围配置；登录增加停用校验（后端 `POST/PATCH /accounts`）。
- 经营看板：新增近30天留资走势、留资来源分布、月度签到活跃度等真实数据图表（`/analytics/trends`、`/analytics/channels`）。
- 注册页接入真实注册接口（`POST /auth/register`，默认关闭，配置 `REGISTRATION_ENABLED=true` + 邀请码开启）。
- 数据模型：新增 `users.status`（启用/停用）字段迁移。

## 2026-08-28 ｜ fix: 完善在线发布包和版本日志

- GitHub Actions 推送 `main` 后自动构建 `auto-latest` 发布包。
- 支持配置 `GITEE_TOKEN` 后同步创建 Gitee Release 并上传同一发布包。
- 服务器更新脚本优先下载 Gitee 最新 Release，失败时回退 GitHub。
- 修复版本页面更新日志接口失败时整页状态异常的问题。
- 更新日志缺失时返回明确的空日志内容，不再触发 404。

## 2026-08-28 ｜ deploy: 阿里云切换为 oa.nbyimai.com 单域名单站点

- Vue 构建产物与 Laravel `public` 合并，由同一个 HTTPS 域名提供页面、API 和 H5 分享页。
- `oa.nbyimai.com` 根目录切换为 Laravel `public`，启用 PHP 8.4 和可信 SSL。
- `/api/*`、`/up` 进入 Laravel，其他路径回退 Vue `index.html`。
- 删除旧 `oaapi.nbyimai.com` 宝塔站点及旧目录，保留数据库和统一站点备份。
- 发布脚本改为生成 `app/public` 单站点结构，并使用锁文件安装依赖。

## 2026-08-28 ｜ feat: KeepYoga 多表同步与会员经营清单数据闭环

- 新增 KeepYoga 会员基础、会员卡、团课签到、私教签到多表同步。
- 按 `member_id` 与预约记录 `m_id` 精确关联，补齐主卡、剩余课次、到期日、最近到店和三个月出勤字段。
- 按有效卡状态过滤资产，排除过期、退卡、停卡和体验/员工占位卡。
- 会员管理改为使用最近三个完整自然月的已签到数据计算五张经营清单。
- 修复空到店日期误判待复活、出勤降低/预流失重叠、VIP 阈值边界等问题。
- 同步请求按时间窗口处理，避免 PHP 128MB 内存溢出。
- 同步页面展示会员、卡项、预约和签到数量，并在门店同步失败时显示部分失败。
- 统一任务、审批、审计和同步批次分页响应。
- 修复今日预约快照写入权限和输入范围校验。
- 清理后端 Laravel 默认 README，更新项目部署和 KeepYoga 配置说明。

## 2026-08-27 ｜ fix: 客户数据分页、导入幂等与双店数据可见性

- 修复 KeepYoga 导入只识别单一会员字段、已存在记录虚报更新的问题。
- 修复客户池、会员管理、留资和同步批次的分页与响应契约问题。
- 修复多账号门店数据范围和 KeepYoga 导入权限校验。
- 两台生产服务器完成会员数据回填和同步链路验证。

## 2026-08-26 14:01 ｜ feat: 阶段1启动——Laravel 12最小后端（认证/留资/会员五清单/任务/审批/审计API）+ 前端双通道切换开关

- 修改 admin-web/src/api/auth.ts
- 新增 admin-web/src/api/backend.ts
- 修改 admin-web/src/api/yimai.ts
- 修改 admin-web/src/store/modules/user.ts
- 修改 admin-web/src/views/yimai/members/index.vue
- 新增 backend/.editorconfig
- 新增 backend/.gitattributes
- 新增 backend/.gitignore
- 新增 backend/README.md
- 新增 backend/app/Http/Controllers/AuthController.php
- 新增 backend/app/Http/Controllers/Controller.php
- 新增 backend/app/Models/AppSetting.php
- 新增 backend/app/Models/Approval.php
- 新增 backend/app/Models/AuditLog.php
- 新增 backend/app/Models/Customer.php
- 新增 backend/app/Models/Lead.php
- 新增 backend/app/Models/Task.php
- 新增 backend/app/Models/TrainingPlan.php
- 新增 backend/app/Models/User.php
- 新增 backend/app/Providers/AppServiceProvider.php
- 新增 backend/artisan
- 新增 backend/bootstrap/app.php
- 新增 backend/bootstrap/cache/.gitignore
- 新增 backend/bootstrap/providers.php
- 新增 backend/check.php
- 新增 backend/composer.json
- 新增 backend/composer.lock
- 新增 backend/config/app.php
- 新增 backend/config/auth.php
- 新增 backend/config/cache.php
- …等共 73 个文件


## 2026-08-26 13:08 ｜ docs: 重写项目README，移除模板遗留文档；分支master更名main

- 新增 README.md
- 删除 admin-web/README.md
- 删除 admin-web/README.zh-CN.md
- 修改 sync.ps1


## 2026-08-26 13:03 ｜ chore: 接入GitHub远程仓库并启用自动变更日志

- 修改 sync.ps1
