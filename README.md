# 一麦管理工作台

> 一麦瑜伽 · 双店内部经营与服务执行平台
>
> **当前版本 v3.3.1** · 版本记录见 [CHANGELOG.md](CHANGELOG.md)

面向**超管 / 店长 / 老师 / 新媒体**四类角色的内部工作台：把客资承接、体验成交、会员维护、续费预警、任务审批放进一个闭环，AI 内容生成作为效率工具嵌入。

生产部署采用单域名单站点：Vue 构建产物位于 Laravel `public`，页面和 `/api` 共用同一 HTTPS 域名。

---

## 版本记录

每一次迭代在 [CHANGELOG.md](CHANGELOG.md) 留痕，并同步更新本 README 与 [DEPLOY.md](DEPLOY.md)。线上版本见工作台「系统管理 → 版本更新」。

| 版本 | 日期 | 摘要 |
|------|------|------|
| v3.3.1 | 2026-09-23 | **手机端列表页可用宽度回归修复**。v3.3.0 引入的 `.list-page` 在手持设备（≤768px）下给 `.list-page--fill` 静默加回 12px 页面内边距：媒体查询里的 `.list-page{padding:12px}` 与媒体查询外的 `.list-page--fill{padding:0}` **权重相同（0,1,0）且排在后面**，于是 `--fill` 列表页在手机上每边各缩 12px、合计损失 24px 可用宽度，而桌面端仍是 0 —— 两档宽度不一致。实测后果（390×844 留资管理）：卡片内容宽 288 → 264px，卡片底部「编辑 / 变更记录 / 更多」三个按钮（`min-width` 84px + `gap` 8px，合计 264px）**从 1 行挤成 2 行**，卡片高 397 → 449px、整页 6029 → 6811px，分页器与卡片右边界错位；会员管理（指标行由 1 行变 2 行、页面 3032 → 4844px）、人员管理、客户经营池、任务管理、人员操作日志、系统日志、模型生成记录、价格审批、新客培养等**共 10 个同类页面**同样受影响。修法：在手持设备分支给 `.list-page--fill` 显式补回 `padding: 0`（该不变量在 `--fill` 分支已声明为「必须」，只是分档时漏掉），内容页仍保留 12px 收紧档。桌面端不受影响（本就为 0），卡片操作行恢复 1 行、卡片宽度与桌面端一致 |
| v3.3.0 | 2026-09-22 | **三项判定缺陷修复 + 全站表格高度统一 + 新增薪酬计算栏目**。① **会员分层此前从未真正生效**：`layer` 只有两处硬编码写 `'P4'`（`KyMemberSyncService`），P0–P3 永远不会被赋值 —— 现改为逐会员实时判定并按优先级 `P0>P1>P2>P3>P4` 落库，同步后自动重算，新增 `rebuild:layers` 命令（支持 `--dry-run`）。② **「待续费」判定不精准**：卡项被分为在用/未开卡/余额未知/已耗尽四类，其中「余额未知」不再被当成「课时用完」（那是拿缺失数据下结论），期限卡按到期日判定并显式降级告知；两处证据集改**正向判定**（guard 用「除未开卡外无其他卡项」，`hasAsset` 用「`cards_list` 非空即有资产」），修掉「加一张未开卡卡项就把临期信号整个抹掉」的旁路。③ **`P5` 语义收口为单一谓词**（`layer='P5' AND NOT ky:`），修复新媒体角色可读到 `ky:` 来源真实会员 PII 的越权面。④ **全站表格高度自适应**：写死 `max-height` 清零（HEAD 实为 `<ElTable>` 11 处 + `<ArtTable>` 1 处），改为按视口计算（斜率 1.000），修复内容页表格**无界增长**（同步页原每秒长 60px）、`.el-alert` 后渲染致高度不重算（分页器被挤出视口）、以及 1440/1460px 下留资管理筛选条换行。⑤ **新增「薪酬计算」栏目（仅超管可见，11 个端点全部 `requireSuper`）**：读取每位老师的课时数（按课次口径去重，非预约行数；时长从课程名解析 45/60 分钟，解析不出标 `assumed_60` 不猜档）、老师课时费与**身份标签**设置（身份标签对应不同薪酬计算方式）、按月导入门店业绩明细（`.xlsx`，含 299 活动卡剔除、销售别名解析、异常行不静默），逐笔 half-up 舍入后求和；桌面端与手机端卡片化均已适配。**升级后须执行 `php artisan migrate`，并配置 `PAYROLL_MASTER_XLSX`（否则薪酬页为空），服务器需启用 `php_zip` 扩展（否则业绩导入报错）**；详见 [DEPLOY.md](DEPLOY.md) |
| v3.2.2 | 2026-09-21 | **课型映射纠错**：`course_type` 数字↔课型对应被 v3.2.0/v3.2.1 改反了，本版改回 **`2`=私教课（「定制私教」）/ `3`=精品课（「私教小班」）/ `1`=团课**，并新增迁移 `2026_09_21_000004` 按 `raw.course_type` 逐行重算历史 `course_kind`（幂等）。该映射是授课老师「我的学员」与会员/客资可见性的**授权判据**，反向会导致老师的真实私教学员消失、同时把小班学员当私教学员看到（越权可见他人学员）；**已升级到 v3.2.0/v3.2.1 的请尽快升级本版纠正数据，升级前先备份数据库**。判定依据为用户第一手确认 + 同文档逐行实测例子 + 三处独立来源交叉验证，上一版误把设置页显示顺序当成了数字绑定；完整复盘已写入 `courseKindFrom()` 注释 |
| v3.2.1 | 2026-09-21 | 新增**新媒体业绩机制**：2 个月时效（留资当月+次月，例 9.1 留资 → 10.31 前有效）内的到店奖励 20 元/人、核销提成 = 当月线上新客成交率 × 当月核销金额，线下渠道不计入；提成系数分母为当月全部到店新客（例：10 月留资 20/到店 10/成交 6，另有 9 月留资 2 人在 10 月到店、1 人成交 → 到店奖励 `(10+2)×20 = 240`，系数 `(6+1)÷(10+2) = 58.33%`），统计口径为自然月且区间标注在卡片上，日期无法判定者不计入并单独计数；留资管理新增「每页显示数量」（20/50/100/200）；修复留资管理桌面端表格底部空白（根因是表格高度被写死 520px，改为视口自适应后可见行数 7→9）；营销工具页手机端三个子工具自适应卡片化（按钮被裁切点不中 3→0、触控全部 ≥44px、最窄表单控件 40→46px，桌面端逐值不变）；`update.sh` 增加防呆——检测到站点根或其父目录存在 `.git` 即中止，避免在开发仓库内执行时发行包静默覆盖 `backend/`（生产站点不带 `.git`，不受影响） |
| v3.2.0 | 2026-09-21 | 安全收口为主：**销售分享码改由服务端签发**（原先由前端编译期常量决定，任何人可从前端产物读出并直接请求公开接口，学员案例原文随之出网），历史非服务端签发的码不再下发，**升级后各门店需重新开启一次 H5 分享**；销售分享写路径越权（超管 publish 改写他人行 / disable 一次停用全库 / 同名账号互相接管）修复，读写选择器彻底拆开；分享内容清洗 fail-closed（逐层白名单 + 容器类型不符时丢弃，未授权案例不再出网）；课型映射一度改为 `2=小班 / 3=私教` 并回补历史 `course_kind`（**该方向经复核为误判，已于 v3.2.2 纠正为 `2=私教 / 3=精品课`**），兜底收口到 `courseKindFrom()` 单一入口；phones 授权通道修复（手机号关联恒 false 致老师静默漏看仅凭手机号关联的学员）；手机端 UI 重设计（6 页宽表格降级为卡片、触控 44px、修掉弹窗内与窄屏盲区）；两处整页白屏崩溃修复（`status.toLowerCase()` / `action.includes()` 遇 null）；分享页缺数据改为优雅降级（区分「数据不全」与「链接已失效」）；新增「归属异常」面板供超管处置孤儿分享，以及版本更新页常驻升级提示；`.gitignore` 加固 |
| v3.1.65 | 2026-09-19 | 新客培养概览六张卡片改为可直接点击筛选（与日期/门店/课型筛选叠加生效；卡片数字改从当前列表算出，修掉与后端 `summary` 的口径不一致）；修复 `make-release.sh` 打包成功后仍报失败并返回非 0（全角括号被并进变量名，`update.sh` 同一处写法一并修）；Gitee 发行包上传超时改为「连续 180s 无进展才判死」的停滞超时（原 600s×5 次重试真卡住时白烧约 52 分钟） |
| v3.1.64 | 2026-09-17 | 全量审计修复：新媒体角色留资越权收口（此前可拉双店全部客资）；到店/成交口径抽 `VisitMetrics` 统一（老师侧此前漏掉"只勾了体验卡片"的到店）；老师工作台「我的客资」与客资趋势修掉恒为 0；手机号查重归一（带分隔符不再漏报）；备份默认不再打包 `.env`、S3 对象键补校验、恢复逐表校验行数；在线升级加数据库+代码快照与失败自动回滚；CI 增加测试与类型检查门禁；演示种子改构建期可摇掉（发布产物内假会员数据清零） |
| v3.1.63 | 2026-09-16 | 留资管理「上课老师」列补全：原先读的是旧版单节字段，而老师实际填在逐节卡片里，导致该列恒为空；同时该字段是老师的归属依据，只填卡片会让老师看不到自己上过的课，一并修好 |
| v3.1.62 | 2026-09-16 | 归属 id 落地：6 张归属表补 `*_user_id` 并与姓名双写、迁移按姓名/别名回填；归属判断改为「id 或姓名取并集」（避免 id 悬挂时静默丢数据）；门店→随心瑜 venue_id 从 4 处硬编码收敛为配置；映射面板新增悬挂 id 待清理项，补别名后自动回填历史行 |
| v3.1.61 | 2026-09-16 | 人员归属改为「按账号解析姓名」：新增 `staff_aliases` 别名表与 `staffNames()`，全站归属过滤不复用单一姓名字符串；修掉 `/api/me` 的 userName（昵称）被写进归属列导致数据对本人消失；人员管理新增「归属映射」面板，可直接看到归属悬空的姓名并补映射 |
| v3.1.60 | 2026-09-16 | 移动端适配：宽表格统一降级为卡片（固定列会吃满手机可见宽度，横滑也看不到数据）、断点收敛为单一来源、触控尺寸提到 44px、修复 641–860px 弹窗溢出与两处 hover 菜单、课后分析向导与今日待办移动端打磨；桌面端零回归 |
| v3.1.59 | 2026-09-16 | 修复训练计划只有创建人可见（改按角色可见范围：超管全部 / 店长本店 / 老师自己创建的），为此补门店列，且店长改计划不夺归属；训练计划分享链接打不开（公开接口缺 status/share 字段，该页面从未成功打开过）；线上到店判定放宽为「核销时间或留资日期任一落在区间」 |
| v3.1.58 | 2026-09-16 | 到店补第三处来源（留资体验课卡片勾了「已上课」）并给出来源构成 visitBreakdown；登录页精简、移除滑动验证；默认关闭多标签页、开启顶部进度条；全局移除中英文切换；手机号全量显示（原多处只显示尾号） |
| v3.1.57 | 2026-09-16 | 修复老师工作台整页 500（Eloquent 集合 intersect 对字符串调 getKey）及其身份去重丢字段致到店/成交失真；课后分析对客页分享链接缺 `#` 打不开；训练计划改逐条读写（原整表替换会删掉服务端刚生成的计划）、删除改显式 deletedIds；课后分析按渠道区分老会员与新建客资 |
| v3.1.56 | 2026-09-15 | 账号支持多角色叠加（可见范围取并集，改角色立即失效登录态）；体测报告解析归档并自动带出训练观察项、课后分析可一键流转训练计划；修复同步完成时间比批次号早 8 小时；修正到店口径与会员出勤为三个连续 30 天滚动窗口；看板缓存键补上授权门店避免串数据 |
| v3.1.55 | 2026-09-15 | 老师角色拆分为服务老师（会籍顾问）/ 授课老师（私教主教练）两条线；体验课课后分析 + 训练方向 P0 上线（非 AI 规则引擎按「类型 × 本次观察」组装、红线硬拦截不产出方案、对客页 `/s/post-class`）；老师工作台课时改读真实排课事实并新增 course_kind 区分钟课型 |
| v3.1.54 | 2026-09-15 | 修正线上新客成交率分母（改为「线上成交 ÷ 线上到店」）；新媒体板块统一到「新媒体登记来源」单一口径；同步执行结果写入运行日志；生产补齐 Laravel 调度器（此前 ky:autosync、日志清理、定时备份全部未执行） |
| v3.1.53 | 2026-09-12 | 修复自动备份开启后无法关闭（备份配置接口返回下划线键名而前端契约是驼峰，保存恒 422）及状态卡片数据恒为空（同一根因） |
| v3.1.52 | 2026-09-12 | 备份下载增加 loading 与「正在从远端拉取」提示（原点击无反馈）、超时放宽至 5 分钟；在线更新遇 502/504 给出 PHP-FPM 排查指引而非笼统报错 |
| v3.1.51 | 2026-09-12 | 修复 MySQL 下「立即备份」报 reset() 引用传参错误（array_map 传值）——该分支仅生产 MySQL 执行、SQLite 测试未覆盖，已在真实 MySQL 补验备份/校验/恢复全流程 |
| v3.1.50 | 2026-09-12 | 备份远端新增 S3 兼容对象存储（MinIO / 腾讯云 COS / Cloudflare R2 / B2 / 阿里云 OSS 兼容模式），AWS SigV4 用 PHP 原生实现、零新增依赖；备份配置不再回显任何密钥明文 |
| v3.1.49 | 2026-09-12 | 修复 WebDAV 测试连接报 500 吞掉真实原因（未选类型 / 地址不通 / 超时 / 证书校验失败），改为可操作的中文提示并写入系统错误日志 |
| v3.1.48 | 2026-09-12 | 数据备份与恢复上线（本地 + WebDAV/NAS 远端、GFS 保留策略、自动备份、完整性校验、恢复前快照）；KeepYoga 同步拆分「增量 / 全量」；修复已取消的留资体验课卡片仍进入今日待办 |
| v3.1.47 | 2026-09-12 | 营销工具新增「朋友圈防折叠」改写（自带最长连续相同片段质检，≥10 字红字提醒）；今日待办体验课「已接待 / 爽约」回写留资状态并在卡片标记；朋友圈文案新增自定义字数档位 |
| v3.1.46 | 2026-09-10 | 修复在线升级后全站 500：helpers.php 增加 bootstrap require_once 兜底加载 + update.sh 重建 composer autoload（生产已补登记并恢复） |
| v3.1.45 | 2026-09-10 | 修复 v3.1.44 分包循环引用导致生产整站白屏（destr/deep-pick-omit 归入 vue-vendor 分包）；在线更新在宝塔禁用 proc_open 时给出明确指引而非「Server Error」 |
| v3.1.44 | 2026-09-10 | 性能与架构专项：核心表 16 索引、会员/客户页真服务端分页+轻量接口、五清单/看板缓存、KY 同步异步化（受理即返回+轮询）+每日定时同步、routes/api.php 拆分 17 控制器、前端分包主包 1385K→108K、修复 PHP 8.5 配置崩溃隐患 |
| v3.1.43 | 2026-09-09 | 修复 PHP 8.5 下登录恒报「账号或密码错误」：数据库配置用了已弃用的 `PDO::MYSQL_ATTR_INIT_COMMAND`，PHP 8.5 的 deprecation 警告随 JSON 响应输出导致前端解析失败；改用 `Pdo\Mysql::ATTR_INIT_COMMAND`（带旧版回退） |
| v3.1.42 | 2026-09-09 | 修复新媒体双店账号工作台全部数据为空；按 venues 安全筛选双店/单店，并修正平台图、零值、漏斗、错误态和快速切换旧响应覆盖 |
| v3.1.41 | 2026-09-09 | 修复生产 PHP-FPM 缺少 finfo 时 Flysystem 初始化导致随心瑜多表同步在快照阶段失败，快照改为受控私有目录原生文件读写 |
| v3.1.40 | 2026-09-09 | 自愈生产库半迁移状态；安装完成一次性展示账号密码并清理登录路径；同步增加重复分页熔断、无变化免写、单任务锁和 gzip 快照，降低 2H2G 系统盘压力 |
| v3.1.39 | 2026-09-09 | 修复随心瑜多表全量导入唯一键回归并增加迁移前置提示；全新安装包自动进入安装页；初始超管密码改为至少 6 位并在锁定安装器前验证可登录凭据 |
| v3.1.38 | 2026-09-08 | 修复刷新掉登录（v3.1.36 persist 漏存 isLogin 的回归）；首页新增「留资登记成交」卡，按成交日统计、改留资成交即联动，线上成交转化率口径保留 |
| v3.1.37 | 2026-09-08 | 新客培养栏目：统计入会近 N 天会员，私教/小班/团课三分显示已签到/预约/爽约节数与养成进度、待激活预警、预约主题；养成目标节数分类可调；同步落库入会/到访时间 |
| v3.1.36 | 2026-09-08 | 审计收尾：token 14天过期、AI异常不回传上游体、同步快照行锁+N+1消除、任务流转加锁、看板日期下推SQL、五清单一次扫描、audit_logs索引+默认保留、/leads服务端分页、锁屏密码不落盘、看板不吞错 |
| v3.1.35 | 2026-09-08 | 安全加固：新媒体跨店越权、H5 分享越权覆盖、AI 日配额、登录/公开端点限流、SSRF 关重定向、审批并发、配置单例竞态、同步互斥与僵尸回收、update.sh 供应链加固；修复体验课核销金额小数无法保存、前端时区取日与异常吞错 |
| v3.1.34 | 2026-09-08 | 版本更新页新增远端最新发行更新记录，升级前即可查看；本次更新与当前已安装完整历史分区展示，并补齐三级标题渲染 |
| v3.1.33 | 2026-09-08 | 个人中心新增昵称/邮箱/手机号与头像移除；KeepYoga 合同总数修复、同步生成私有 CSV 历史快照并可下载；系统运行/错误日志与人员操作日志拆分、保留策略；模型生成记录全链路与超管筛选；老板工作台错误态和单店体验完善 |
| v3.1.32 | 2026-09-08 | 留资跟进时限从首次体验课起算（第一节取消则留白）；体验课卡片可勾选已取消、移除按钮始终可用；留资新增删除功能（按编辑权限+留痕）；核销/成交金额支持小数（decimal 迁移+校验修正）；工作台「未完成合同签署」可点击查看未签名单 |
| v3.1.31 | 2026-09-07 | AI 营销工具升级：发圈/小红书人设云端保存、销售转化主题与首条回复建议、历史生成记录；个人中心回归真实账号信息（头像/专业/改密） |
| v3.1.30 | 2026-09-07 | 今日预约手机号回填会员档案，保证全量号码展示 |
| v3.1.29 | 2026-09-07 | 更新脚本收尾平滑重载 PHP-FPM，修复新增类短暂 500（今日待办事件复盘） |
| v3.1.28 | 2026-09-07 | 日志接口兼容 daily 通道：列出/读取按天日志文件 |
| v3.1.27 | 2026-09-07 | 新增超管远程日志查看接口 /system/logs（免 SSH 定位线上问题） |
| v3.1.26 | 2026-09-07 | 修复线上今日待办接口 500：迁移幂等可重试（cards_list/todo_actions 半成功状态自愈）+ 待办加载失败显式报错 |
| v3.1.25 | 2026-09-06 | 今日待办升级为可执行闭环：工作台概要化 + 客户经营新增完整「今日待办」页（逐项处理、标记后当天消隐并自动流转：留资状态/最近触达/续课计划/下降处置）；待办显示全量手机号；剩余课时列逐卡展示有效卡项明细 |
| v3.1.24 | 2026-09-06 | 待续费多卡口径修复（全部有效卡汇总，废卡不误报、未开卡计入库存、时间卡不误判 0 节）+ 阈值拆分为次卡节数/次卡占比/到期天数/有效期占比四项 |
| v3.1.23 | 2026-09-06 | 合同签署口径修复（status=1+双方签署位，解决数量恒为 0）+ 已过期提醒；新增随心瑜经营概览卡（收入/耗卡/剩余资产/活跃·风险·沉寂·流失/访客转化）；今日待办阈值与客户管理实时同步展示 |
| v3.1.22 | 2026-09-06 | 工作台新增「今日待办」栏目：今日预约会员明细（含课型/老师/签到状态）+ 待续费/流失风险/生日关怀/体验课/新客首响/今日任务分组汇总，按角色门店隔离；会员新增生日字段与维护入口 |
| v3.1.21 | 2026-09-05 | 修复店长工作台三分显示 0 的误导：旧快照无 todayKinds 时返回 null 并显示中性提示；约课/班次 KPI 无数据时给出可读说明 |
| v3.1.20 | 2026-09-02 | 门店经营预约与上课班次按私教/小班/团课三分展示（course_type 2/3/1 判定），今日预约快照新增 todayKinds |
| v3.1.19 | 2026-09-02 | 人员管理补齐姓名字段与删除账号；门店经营约课人数/上课班次按私教·团课小班拆分展示；售卡张数口径提示统一 |
| v3.1.18 | 2026-09-02 | 门店经营售卡张数/金额改为随心瑜会员卡表实收口径（ky_cards 事实表），不再依赖工作台留资手动登记；售卡金额提示文案同步更新 |
| v3.1.17 | 2026-08-30 | 第一阶段安全加固：AI接口封堵SSRF与密钥泄露；留资按角色门店字段级权限；停用/重置即时撤销Token；工作台待办按角色门店隔离；训练计划跨账号隔离；401统一清会话；审计日志服务端分页；审批角色与状态过滤修复；PHP要求统一8.4.1+；权限矩阵与AI安全测试（25项/112断言） |
| v3.1.16 | 2026-08-30 | 新增完整宝塔首次安装包：内置生产 vendor、Web 安装向导和自适应在线更新脚本；自动配置数据库/APP_KEY/超管，安装后锁定并删除入口；与在线升级包分开发布 |
| v3.1.15 | 2026-08-30 | AI助手头像换为一麦Logo；修复编辑留资500；全面审查并完善留资/会员/任务/审批错误反馈、工作台接口容错、审批状态机和门店隔离、接口字段校验及版本判断 |
| v3.1.14 | 2026-08-30 | 累计购买改为会员卡实收金额（deal_price 求和）、VIP标准改为3万元；修复留资保存无响应（字段白名单+前端错误提示）；体验课时间框易用性；店长端隐藏门店筛选只看本店；按钮文案改为调整标签阈值 |
| v3.1.13 | 2026-08-30 | 一麦Logo favicon；门店经营改为约课/签到班次/登记售卡；线上新客成交率、合同双方待签状态、角色分级可点击通知中心 |
| v3.1.12 | 2026-08-29 | 续费评估改为系统数据+人工判断固定100分；企微有效沟通替代朋友圈；列表展示评估和下一步动作，评估历史自动联动角色任务与店长验收 |
| v3.1.11 | 2026-08-29 | 修复随心瑜计数代理与零快照；团课/私教预约事实落库；双店看板按门店独立汇总，约课/体验/到店使用预约与签到真实口径 |
| v3.1.10 | 2026-08-28 | 留资新增弹窗体验课卡片化：每节体验课独立记录时间/主题/老师/券信息与核销金额，核销自动汇总，旧单节数据兼容 |
| v3.1.9 | 2026-08-28 | 人员管理可编辑角色/门店、注册补登录态；客户/会员/留资搜索姓名与手机号拆分、留资范围随门店动态；操作留痕补IP与设备、清阶段残留；同步实时数据错误提示与今日预约解析兼容、历史批次同步明细 |
| v3.1.8 | 2026-08-28 | 留资下单平台/微信/券码次数与体验课节次卡片；留资联系方式与日期筛选、爽约状态、手机号命中检测；新媒体仪表盘平台金额树状图与本月成交；个人中心404与大模型非超管不可用修复 |
| v3.1.7 | 2026-08-28 | 会员门店筛选全页签+门店列+动态范围；五清单阈值全可调；AI流式实时显示与排版；新媒体空菜单修复；工作台真实数据；增量导入 |
| v3.1.6 | 2026-08-28 | 留资「体验课类型」字段、会籍顾问下拉全角色可见、表格滚动修复、AI 流式输出 |
| v3.1.5 | 2026-08-28 | 累计购买洗板修复（私教节数=剩余+已用）、全量导入内存修复、会员管理门店筛选 |
| v3.1.4 | 2026-08-28 | 升级脚本兼容宝塔 chattr 锁定文件，收尾属主整理容错，修复升级失败 |
| v3.1.3 | 2026-08-28 | 后台在线升级可用（解除 proc_open 限制）、更新日志随版本同步、README/文档同步留痕 |
| v3.1.2 | 2026-08-28 | 会籍顾问统一口径：会员/经营池/留资/同步全系统统一命名，按手机号自动匹配服务老师 |
| v3.1.1 | 2026-08-28 | 会员管理会籍顾问独立列与筛选（含待分配） |
| v3.1.0 | 2026-08-28 | 任务中心/客户端浏览审批/客户360/经营看板/人员管理等补全，AI配置存服务端数据库 |

## 技术栈

| 层 | 选型 |
|----|------|
| 前端 | Vue 3.5 + TypeScript + Vite 7 + Element Plus + Tailwind CSS 4 |
| 状态 | Pinia（仅本地演示数据和界面状态；生产业务数据由 Laravel 持久化） |
| 图表 | ECharts 6（art-design-pro 封装组件） |
| AI | OpenAI 兼容协议，支持 DeepSeek / 通义千问 / Kimi / GLM / 豆包 |
| 数据接入 | 随心瑜云 KeepYoga（Laravel 服务端只读代理与多表聚合） |
| 后端 | Laravel 12 模块化单体 + Sanctum + MySQL 8 |

## 功能模块

```
一麦工作台
├── 工作台          四角色各自的数据仪表盘（KPI+趋势图+渠道对比+漏斗）
│                   ├ 随心瑜经营概览：收入/耗卡/剩余资产/会员活跃/访客转化直读上游口径（店长及以上）
│                   └ 今日待办：今日预约会员（含服务提示）+ 待续费/流失风险/生日关怀/体验课/新客首响/今日任务（阈值随客户管理同步）
├── 会员管理        五清单引擎：待续课/出勤降低/VIP/预流失/待复活（口径：卓越店长训练营）
│                   ├ 多卡口径：按全部有效卡汇总判定，废卡不误报；未开卡计入库存；次卡/时间卡阈值分设（节数、占比、到期天数、有效期占比）
│                   ├ 会籍顾问独立列 + 筛选（未分配红标；按手机号匹配留资服务老师，全系统统一「会籍顾问」口径）
│                   └ 30天续费经营评估（系统签到/卡项自动计分 + 人工服务判断 + 角色任务闭环）
├── 留资管理        新媒体录入→店长分配→会籍顾问跟进；下单平台/微信/券码次数/体验课节次卡片，手机号命中提示，全字段变更留痕
├── 客户经营池      超管全景视图（KeepYoga会员、卡项、签到数据聚合，会籍顾问匹配联动）
├── 任务中心        新建任务/认领执行/分配负责人/店长验收 全流程
├── 价格审批        发起审批→店长初审→超管终审→关联成交，未审批不能标记成交
├── 训练计划        AI生成草稿→老师确认→H5分享页（含对比照相册）
├── 谈单工具        门店资料/产品价目/教练/案例管理 + 免登录H5分享页
├── 营销工具        朋友圈/小红书文案生成（大模型API可配置，人工确认后发布）
│                   ├ 发圈/账号人设按账号云端保存（角色/专业/客户画像，品牌固定「一麦」·城市「宁波」）
│                   ├ 主题模板（含销售转化）、首条回复建议、历史生成记录（每平台最近300条）
│                   └ 个人中心（右上角头像）：头像上传/手机号/专业/改密，资料自动带入人设
├── KeepYoga同步    只读接入·实时计数·今日预约快照·会员卡/预约事实/出勤多表同步（仅超管）
├── 操作留痕        全系统修改类操作审计，超管可溯源
├── 人员管理        开通新账号/停用启用/重置密码/角色与门店范围（仅超管）
└── 模型配置        大模型API接入管理，密钥存服务端数据库、多设备共用（仅超管）
```

## 快速开始

```bash
cd admin-web
pnpm install          # 需 Node ≥ 20.19，pnpm ≥ 8.8
pnpm dev              # http://localhost:3006
```

### 本地演示认证

生产环境必须使用 Laravel 后端认证。只有显式设置 `VITE_DEMO_PASSWORD` 时，本地前端模式才启用演示账号；演示账号和密码不得用于生产。

### KeepYoga 数据接入

生产和后端模式下，在服务器环境变量中配置，或登录后台后进入“KeepYoga同步 → 登录账号设置”：

```env
KY_PHONE=随心瑜登录手机号
KY_PASSWORD=随心瑜登录密码
```

配置后以超管登录 → KeepYoga同步 → 可获取双店实时计数、今日预约快照，并执行会员基础、卡项、团课/私教签到多表同步。

同步会把随心瑜的会员 `member_id` 与卡项、预约记录中的 `m_id` 对齐，并把团课/私教预约幂等保存到 `ky_bookings`。会员管理使用最近三个完整自然月的“已签到”次数计算待续课、出勤降低、预流失等清单；工作台体验/到店优先使用体验预约签到事实；过期、退卡、停卡不作为有效资产。

### AI 大模型配置

超管登录 → 模型配置 → 选择服务商 → 填入 API Key → 测试连接。
未配置时营销工具/训练计划自动降级为本地模板草稿。

## 目录结构

```
一麦工作台/
├── admin-web/              # 管理后台应用（本仓库主体）
│   ├── src/api/            # 业务接口层（后端 API 与本地演示适配）
│   ├── src/store/modules/  # Pinia：客资/会员/训练/谈单/AI配置
│   ├── src/views/yimai/    # 一麦业务页面
│   ├── src/views/sales-share/   # 谈单H5分享页（免登录）
│   ├── src/views/training-share/ # 训练计划H5分享页（免登录）
│   └── vite-plugins/keepyoga.ts # 仅本地兼容代理；生产使用 Laravel 后端
├── sync.ps1                # 一键同步：提交+变更日志+推送GitHub
├── CHANGELOG.md            # 自动生成的更新日志
└── *.docx / *.md / *.xlsx  # 架构书、需求报告、业务数据
```

## 版本同步

```powershell
.\sync.ps1 -m "feat: 本次改动说明"   # 暂存→写CHANGELOG→提交→推送
```

## 当前状态与阶段路线

**功能主线**见 [docs/体验课课后分析-产品规划.md](docs/体验课课后分析-产品规划.md)（P0 已完成，P1 进行中）。
下面这份清单描述的是**工程侧**的完成度，2026-09-17 核对过一遍：

- **已完成**：Laravel 认证、业务 API、审计落库、客户/留资/同步服务端分页、KeepYoga 服务端凭据、
  多表会员/卡项/签到聚合、五清单真实计算、H5 分享能力；卡项事实表（`ky_cards`）、
  原始预约流水（`ky_bookings`）、任务与审批写入状态机、144+ 项 Feature 测试与 CI 测试门禁。
- **设计选择（不是待办）**：同步任务走 PHP-FPM 进程内后台执行（`fastcgi_finish_request`），
  不引入队列 worker —— 2H2G 机器上不额外常驻进程。
- **仍在完善**：细粒度 Policy（当前权限逻辑集中在 `EnsureUserIsEnabled` 中间件与各控制器的
  `abort_unless`，没有 `app/Policies`）、备份恢复的事务化（目前逐表 truncate + 灌入，
  失败靠「恢复前快照」回滚）。
- **下一步候选**：老师端移动 H5 深化、谈单页发布版本隔离、内容效果归因、课后分析 P1 剩余项
  （按老师/类型的转化看板、顾问衔接卡）。

生产环境使用后端模式。前端本地 Mock 模式**仅用于界面开发**：演示种子数据在
生产构建里会被打包器整块消除（见 `store/modules/yimai.ts` 的 `DEMO_SEEDS`）。

## 项目文档

- [DEPLOY.md](DEPLOY.md) — 宝塔部署、环境配置与生产发布说明
- [backend/README.md](backend/README.md) — Laravel API、KeepYoga 同步和后端运行说明
- [CHANGELOG.md](CHANGELOG.md) — 项目版本与变更记录
- [KEEPYOGA_DATA_MAPPING.md](KEEPYOGA_DATA_MAPPING.md) — 随心瑜上游字段与本地表的对应关系
- `docs/` — 产品与运维方案（**已被 gitignore，不入库**）。含
  `体验课课后分析-产品规划.md`（功能主线与分期）、`仓库历史清理与凭据轮换.md`（待执行的运维方案）
